---
title: "Excel 导出 - 主从"
enterprise: true
---

Excel 导出提供了将主从网格导出到 Excel 的方法。

## 导出到单个工作表

默认情况下，导出主网格只会导出主行。如果要在导出中包含从行，请使用 `getCustomContentBelowRow` 回调生成从行的表示形式，该表示形式将插入到导出中的主行下方。

渲染和导出主从内容之间存在重要区别。当您在 UI 中展开主行时，会创建一个新的网格实例来渲染从行，这意味着您拥有网格的完整功能来对从数据进行排序、过滤和格式化。

导出时，表示行的原始数据对象传递给 `getCustomContentBelowRow`，后者返回要插入导出的样式化内容。在这种情况下，不会为从行创建单独的网格实例。这确保了即使有大型主从数据集也能获得良好的导出性能。但是，如果您的 `detailGridOptions` 包含值获取器、值格式化器、排序、过滤等，并且您希望这些出现在导出中，则必须在 `getCustomContentBelowRow` 内部应用它们。

{% note %}
由于从网格是完整的网格实例，通过从网格上的右键上下文菜单触发导出将仅对从网格进行正常导出。如果这对您的应用程序不合适，您可以在上下文菜单中禁用导出项，或将其替换为触发主网格导出的自定义项。
{% /note %}

下面的示例演示了如何导出主数据和从数据。

{% gridExampleRunner title="导出主从数据" name="single-sheet"  exampleHeight=550 /%}

## 导出到多个工作表

请注意以下内容：

-   `Master Detail` 数据仅适用于 `expanded` 节点，有关更多信息，请参阅[从网格](./master-detail-grids/)。
-   `RowBuffer` 设置为 **100**，以便所有从网格都可用。
-   `Detail Grids` 导出到不同的工作表。

请注意以下内容：

-   在这种情况下，我们不使用上述带有 `getCustomContentBelowRow` 的方法，因此网格将按原样导出。这意味着只有当主行展开时，才能导出主级别行的从数据。`groupDefaultExpanded` 属性设置为 1 以展开加载的主行。有关更多信息，请参阅[从网格](./master-detail-grids/)。

-   `RowBuffer` 属性设置为 100 以确保至少加载 100 个主行，以便它们可以展开。如果您有超过 100 个主级别行，则需要相应地设置此值以覆盖所有主行，以便它们可以展开并且其从数据可以包含在导出中。

-   每个从网格都导出到 Excel 文件中的单独工作表

{% gridExampleRunner title="Excel 导出 - 带主从的多个工作表" name="multiple-sheets" /%}

---
title: "服务端行模型"
enterprise: true
---

本节概述服务端行模型 (SSRM) 并提供有关何时应使用它的指导。

服务端行模型 (SSRM) 允许应用程序处理非常大的数据集。这是通过以下机制从服务器延迟加载数据来完成的：

1. 在展开组行时从组行延迟加载子记录。
1. 无限滚动浏览数据，随着应用程序滚动加载更多数据。

下面演示了使用 SSRM 查看大数据集：

{% gif imagePath="resources/server-side-showcase.gif" alt="Server-Side Showcase" /%}

在深入了解如何使用 SSRM 的详细信息之前，下一节提供概述并解释何时应使用它而不是默认的[客户端行模型](./row-models/#client-side)。

## 概述

设计基于网格的应用程序时，关键考虑因素之一是需要从服务器向客户端发送多少数据。对此的回答决定了应为网格选择哪种行模型。

### 客户端行模型

最简单的方法是在初始化时响应单个请求将所有行数据发送到浏览器。对于此用例，已设计了客户端行模型。

下图说明了此场景，其中 10,000 条记录直接加载到浏览器中：

{% imageCaption imagePath="resources/in-memory-row-model.png" alt="in-memory-row-model" centered=true constrained=true enableDarkModeFilter=true /%}

使用客户端行模型将数据加载到网格后，网格可以在网格内的数据上进行排序、过滤、分组等操作，而无需应用程序的进一步协助。

客户端行模型仅使用 [DOM 虚拟化](./dom-virtualisation/)渲染当前可见的行，因此行的上限由浏览器的内存占用和数据传输时间决定，而不是网格内的任何限制。

### 服务端行模型

然而，许多实际应用程序包含更大的数据集，通常涉及数百万条记录。在这种情况下，一次性将所有数据加载到浏览器中根本不可行。相反，数据需要根据需要延迟加载，然后清除以限制浏览器中的内存占用。

这正是 SSRM 解决的问题，同时委托服务端操作，如过滤、排序、分组和透视。

下图显示了 SSRM 使用的方法。这里有 1000 万条记录，但记录数量仅受服务端限制的约束：

{% imageCaption imagePath="resources/enterprise-row-model.png" alt="enterprise-row-model" centered=true constrained=true enableDarkModeFilter=true /%}

当用户执行展开组等操作时，网格向服务器发出请求以获取更多数据。

## 功能

您可能会受益于所有这些功能的组合，或者只对其中一部分感兴趣。SSRM 的功能包括：

-   **组的延迟加载：** 网格将仅加载顶级行。组的子行仅在用户展开组时加载。

-   **无限滚动：** 行以块的形式从服务器读回，以提供无限滚动的体验。这允许通过一次只带回一块数据来在浏览器中查看非常大的数据集。

-   **服务端分组、透视和聚合：** 由于数据一次从服务器返回一个组级别，这允许您在服务器上进行聚合，返回顶级父行的聚合结果。例如，您可以在返回的管理器记录中包含"员工计数"作为属性，以说明管理器管理多少员工。

-   **切片和切块：** 假设您的服务端可以构建数据查询，您可以允许用户使用网格 UI 拖动列来选择要分组和聚合的列。用户选择的内容将作为请求的一部分转发到您的数据源。此功能是高级的，需要您进行一些困难的服务端编码，但是完成后，您的用户将获得实时切片和切丁大数据的体验，这以前仅在昂贵的报告工具中可用，现在您可以将其嵌入到您的 JavaScript 应用程序中。

## 全栈示例

为了配合文档中的示例，我们还提供以下全栈示例供参考。
我们建议使用文档中的示例了解服务端行模型，然后使用全栈示例作为参考。

全栈示例如下：

-   [Node.js 连接 MySQL](./server-side-operations-nodejs/)
-   [Java Server 连接 Oracle](./server-side-operations-oracle/)
-   [GraphQL 连接 MySQL](./server-side-operations-graphql/)
-   [Java Server 连接 Apache Spark](./server-side-operations-spark/)

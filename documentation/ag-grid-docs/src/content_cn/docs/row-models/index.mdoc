---
title: "行模型"
---

表格可以配置不同的策略来将行数据加载到表格中，这些策略封装在不同的行模型中。更改表格使用的行模型意味着更改表格用于加载行的策略。

表格提供四种行模型：

1. 客户端
1. 服务器端
1. 无限
1. 视口

客户端行模型处理客户端数据。服务器端、无限和视口行模型处理服务器端数据。以下是每种模型的摘要：

-   ## 客户端

    这是默认设置。表格将一次性加载所有数据到表格中。然后表格可以在浏览器内存中执行筛选、排序、分组、透视和聚合。

-   ## 无限

    这将向用户显示数据，并在用户向下滚动时加载更多数据。如果您想显示大型、扁平（未分组）数据列表，请使用此模型。

    [转到无限行模型](./infinite-scrolling)

-   ## 服务器端 {% enterpriseIcon /%}

    服务器端行模型建立在无限行模型之上。除了在用户向下滚动时延迟加载数据外，它还允许通过服务器端分组和聚合延迟加载分组数据。高级用户将使用服务器端行模型通过服务器端聚合进行数据的临时切片和切块。

    [转到服务器端行模型](./server-side-model)

-   ## 视口 {% enterpriseIcon /%}

    表格将准确告知服务器它正在显示什么数据（第一行和最后一行），服务器将仅提供这些行的数据。如果您希望服务器准确知道用户正在查看什么，请使用此模型，这对于在非常大的实时数据流中进行更新非常有用，服务器仅向查看受影响行的客户端发送更新。

    [转到视口行模型](./viewport)

您使用的行模型由表格属性 `rowModelType` 设置。

{% apiDocumentation source="grid-options/properties.json" section="rowModels" names=["rowModelType"] /%}

## 何时使用

您使用的行模型取决于您的应用程序。以下是一些快速的经验法则：

-   如果使用 **AG Grid 社区版**，如果您想将所有数据加载到浏览器中，请使用**客户端行模型**，如果您想分块加载，请使用**无限行模型**。
-   如果使用 **AG Grid 企业版**，如果您想将所有数据加载到浏览器中，请使用**客户端行模型**，如果您想分块加载，请使用**服务器端行模型**。服务器端行模型是无限行模型加上更多功能。因此，如果您是 AG Grid 企业版客户，您应该优先使用服务器端行模型而不是无限行模型。
-   除非您了解视口行模型的优势以及何时需要它们，否则不要使用**视口行模型**。我们发现许多用户在不需要时使用视口行模型，结果导致应用程序更加复杂。

以下是更详细的经验法则。

-   如果您不确定，请使用默认的**客户端**。表格可以处理大量数据（10万+行）。即使您从服务器返回数千行，表格也只会渲染屏幕上可见的内容（大约40行，取决于您的屏幕大小）。您不会因为数据太多而导致表格崩溃——相反，您的浏览器会在表格出现问题之前耗尽内存。因此，如果您不确定，请先使用客户端行模型，只有在需要时才更改。使用客户端，您可以获得表格为您完成的所有排序、筛选、分组、透视和聚合功能。文档中的所有示例都使用客户端模型，除非另有说明。
-   如果您不想将所有数据从服务器传输到客户端，因为数据量太大无法通过网络传输或无法从底层数据源提取，请使用无限、服务器端或视口。每种模型都以不同的方式从服务器获取数据。
-   使用**[无限](./infinite-scrolling/)**或**[服务器端](./server-side-model/)**从服务器一次返回一个数据块。当用户滚动时，表格将请求更多行。服务器端比无限具有更多功能，并允许行分组、聚合、组延迟加载和数据切片切块。
-   如果您希望服务器准确知道用户正在查看什么，请使用**[视口](./viewport/)**。当您有大量变化的数据并希望在服务器端数据更改时向客户端推送更新时，这是最佳选择。准确知道用户正在查看什么意味着您只需向相关用户推送更新。所有行模型都可以接收更新，但只有视口行模型向服务器提供用户当前在屏幕上看到的行的信息，而无需滚动。

## 行模型比较

以下是所有四种行模型的所有表格功能的快速功能比较。

{% matrixTable dataFileName="row-models"
    columns={
        "feature": "功能",
        "clientSide": "客户端",
        "infinite": "无限",
        "serverSide": "服务器端",
        "viewport": "视口"
    }
    cellRenderer={
        "clientSide": "tickCross",
        "infinite": "tickCross",
        "serverSide": "tickCross",
        "viewport": "tickCross"
    } /%}

## 深入了解行模型

表格遵循 MVC 模式。每个数据项都包装在**行节点**中，然后存储在**行模型**中。表格渲染引擎称为**行渲染器**，它监听行模型的更改并相应地更新 DOM。

下面显示了类图的简化版本，显示与行模型相关的主要类之间的关系。

{% imageCaption imagePath="resources/row-models.svg" alt="与行模型相关的主要类图" centered=true constrained=true enableDarkModeFilter=true /%}

从图中应注意以下几点：

-   表格恰好有一个 `RowRenderer` 实例。`RowRenderer` 包含对 `PaginationProxy` 的引用，它一次请求一行进行渲染。
-   表格恰好有一个 `PaginationProxy` 实例。`PaginationProxy` 将 a) 如果分页未激活，则不执行任何操作，只是将所有请求转发给行模型，或 b) 如果分页激活，则执行分页。`PaginationProxy` 恰好有一个 `RowModel` 实例。
-   您可以配置表格使用任何提供的**行模型**——这就是为什么 `RowModel` 用斜体表示，这意味着它是一个接口，具体实现是您在配置表格时决定的。`RowModel` 包含 `RowNodes` 列表。`RowModel` 可能拥有所有 `RowNodes` 的列表（客户端行模型），或者有一个可以延迟加载 `RowNodes` 的数据源。
-   **RowNode** 恰好引用一个行数据项（客户端应用程序提供行数据项）。`RowNode` 具有关于行项的状态信息，例如是否被选中及其高度。
-   当 `RowNodes` 中的状态发生变化时，`RowModel` 会触发 **modelUpdated** 事件，使 `RowRenderer` 刷新。这有多种原因，例如数据被排序、筛选、打开组或底层数据已更改。

## 分页

分页可以应用于任何行模型类型。每种行模型类型的文档涵盖了该行模型类型的分页。

## 表格数据源

[客户端](./row-models/#client-side)行模型不需要数据源。[无限](./infinite-scrolling/)、[视口](./viewport/)和[服务器端](./server-side-model/)都使用数据源。每种行模型类型的文档解释了如何为特定行模型配置数据源。

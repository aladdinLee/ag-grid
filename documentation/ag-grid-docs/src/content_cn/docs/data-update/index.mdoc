---
title: "更新数据"
---

{% if isFramework("javascript", "angular", "vue") %}
数据在应用程序中可以通过多种方式更改，因此您可以通过多种方式通知网格数据更改。本节介绍了如何使用网格 API 更新网格内数据的不同方法。
{% /if %}

{% if isFramework("react") %}
{% videoSection id="_V5qFr62uhY" title="React 更新数据" showHeader=true %}
数据在应用程序中可以通过多种方式更改，因此您可以通过多种方式通知网格数据更改。本节介绍了如何使用网格 API 更新网格内数据的不同方法。
{% /videoSection %}
{% /if %}

## 更新 vs 编辑 vs 刷新

本页面讨论通过网格 API 更新数据。它不讨论以下内容：

1. 使用网格 UI 在网格内编辑数据，例如通过用户双击单元格并编辑
   单元格的值。发生这种情况时，网格处于控制状态，无需明确告诉网格数据
   已更改。有关如何通过网格 UI 进行编辑，请参阅[单元格编辑](./cell-editing/)。

1. 网格的数据从应用程序的其他地方更新。如果您将数据传递给网格
   然后随后在网格外更改该数据，则可能会发生这种情况。这会使网格的视图与
   其拥有的数据不同步。在这种情况下，您想要做的是[视图刷新](./view-refresh/)以使网格的
   UI 重绘以显示数据更改。

## 更新数据

可以通过以下方式更新网格中的数据：

### 行数据

在网格内更新数据的最简单方法是使用一组新数据替换您提供的数据。这是通过更新 `rowData` 绑定属性（如果使用框架）或调用 `api.setGridOption('rowData', newData)` 来完成的。

有关更多详细信息，请参阅[更新行数据](./data-update-row-data/)。

### 单行/单元格

更新单行或单元格的值。这是通过获取行节点的引用，然后调用 `rowNode.setData(data)` 或 `rowNode.setDataValue(col,value)` 来完成的。

使用此方法无法插入或删除行。

有关更多详细信息，请参阅[更新单行/单元格](./data-update-single-row-cell/)。

### 事务

网格接受包含要添加、删除和更新的行的事务。这是使用 `api.applyTransaction(transaction)` 完成的。

使用事务对大量不频繁的行执行添加、删除或更新操作。

如果您频繁更新行（例如每秒 5 次或更多次更新），请考虑改为使用[高频](#high-frequency)（通过异步事务实现）。

有关更多详细信息，请参阅[事务更新](./data-update-transactions/)。

### 高频

高频（通过异步事务实现）是一种在小段时间内应用许多事务并让网格批量应用所有事务的机制。高频/批处理方法用于需要最快的方式来处理许多连续更新的情况，例如向网格提供更新流。这是使用 API `api.applyTransactionAsync(transaction)` 完成的。

使用异步事务进行频繁的添加、删除或更新操作，例如管理每秒数十、数百或数千次更新的流式更新到网格。

有关更多详细信息，请参阅[高频更新](./data-update-high-frequency/)。

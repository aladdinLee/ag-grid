---
title: "海量行数"
---

由于网格使用[行虚拟化](./dom-virtualisation/)，网格可以显示的行数没有固定限制。但是浏览器对 DOM div 元素的高度有固定限制。鉴于网格使用 DOM 渲染，包含行的 div 具有固定的最大高度，因此对可以使用常规技术渲染的行数有限制。

为了解决这个问题，网格使用一种技术将更多行放入自然可以容纳的 div 中。这种技术是 AG Grid 独有的，我们称之为拉伸，下面将进行解释。

## 问题

每个浏览器对 div 的最大高度都有限制。此限制未发布，因此不同的浏览器以及不同的浏览器版本可能不同。在撰写本文时，在 Chrome v118 上，最大高度为 32,000,000 像素。

网格通过在网格初始化时测试 DOM 来计算 div 的最大高度。要查看网格确定的 div 最大高度，请设置网格属性 `debug=true` 并注意日志输出。一个调试器语句将告诉最大 div 高度，如下所示：

`AG Grid.RowContainerHeightService: maxDivHeight = 32000000`

例如，假设每行高 100px，那么这意味着网格能够处理的最大行数为 320,000，最大 div 高度为 32,000,000。

虽然网格使用[行虚拟化](./dom-virtualisation/)仅渲染行的子集，但它取决于正确设置容器高度以容纳所有行，以便垂直滚动条能够在整个数据集上滚动。

如果网格需要呈现比可能更多的行，则网格使用我们称之为拉伸的技术来完成此操作。

## 拉伸 Div

在下面的示例中，请注意行高度为 100px，最大 div 高度为 32,000,000px（根据测试，但是如前所述，不同的浏览器有不同的最大高度，可以通过查看网格调试输出来验证）。这意味着理论上只能显示 320,000 行。下面的网格配置了 1,000,000 行，每行高 100px，以在实践中显示这一点。

{% gridExampleRunner title="海量数据集示例" name="viewport-big-data" /%}

网格通过根据滚动位置更改每行的垂直位置来实现这一点。每行位置更改多少我们称之为行偏移。

```ts
// 查看我们需要多少额外像素来显示所有行
const additionalPixelsNeeded = combinedRowHeight - maxDivHeight;

// 查看我们垂直向下滚动的百分比
const scrollPercent = scrollY / maxScrollY;

// 我们通过 additionalPixelsNeeded 乘以滚动百分比来偏移行
const rowOffset = scrollPercent * additionalPixelsNeeded;
```

因此，在上面的示例中，当垂直滚动在顶部时，行偏移为 0。可以使用网格的 DOM 检查器检查这一点，并注意行位置是正常的，例如，行索引 0 位于 0px，因此具有 CSS 样式 `transform: translateY(0px)`，行索引 1 位于 100px，因此具有 CSS 样式 `transform: translateY(100px)`。

随着垂直滚动移动，行的位置会根据计算的行偏移进行调整。随着垂直滚动向下移动，行偏移线性增加，直到垂直滚动达到其最大垂直滚动位置。当达到最大垂直滚动位置时，行偏移等于所需高度与可用高度之间的差值。

换句话说，当行不适合时，网格会对垂直滚动应用放大器。网格通过在向下滚动时向上移动行来实现这一点，从而产生滚动移动更快的印象，并允许网格显示比 div 中自然适合的更多行。

该示例还在列预期位置中显示如果网格不拉伸行时的行位置。

## 注意事项

当网格应用行偏移以容纳更多行时，网格似乎会滚动得更快。例如，如果您在滚动板上使用滚动手势或使用鼠标滚轮向下滚动，行将比正常情况移动得更快。这是因为随着网格向下滚动，它正在向上重新定位行。

滚动速度将以与所需额外行空间相同的比例增加。例如，如果需要两倍的空间，则滚动时行似乎会移动两倍。如果需要 x10 空间，则滚动时行似乎会移动 x10。

更快的滚动是实现显示海量行数的副作用。没有办法解决这个问题，因为网格在浏览器的约束内工作，而浏览器对 div 可以具有的最大高度有限制。
如果这使您的应用程序无法使用，请考虑不显示这么多行（首先需要显示数百万行吗？）或使用[分页](./row-pagination/)。

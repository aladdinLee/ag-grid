{% if isFramework("vue") %}

```js
colDef = {
    cellEditor: 'MyCellEditor',
    cellEditorParams: {
        // 使 "country" 值可用于单元格编辑器
        country: 'Ireland'
    },
    // ...其他属性
}
```

{% /if %}

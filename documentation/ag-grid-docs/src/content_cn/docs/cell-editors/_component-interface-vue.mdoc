{% if isFramework("vue") %}

单元格编辑器组件的接口是 `ICellEditor`。当 Vue 组件实例化时，表格将通过 `params` 对象使表格 API、许多实用方法以及单元格和行值可供您使用。

您可以通过常规方法（生命周期钩子、方法等）中的 `this.params` 访问 `params` 对象，以及在使用 `setup` 时通过 `props.params` 访问。

`ICellEditor` 和 `ICellEditorParams` 接口的完整详细信息在下面的 [API 参考](#api-reference)中列出。

{% /if %}

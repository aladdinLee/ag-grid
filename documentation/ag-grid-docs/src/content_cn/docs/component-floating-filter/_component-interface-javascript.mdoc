{% if isFramework("javascript") %}
自定义浮动筛选器组件的接口如下：

```ts
interface FloatingFilterDisplayComp {
    // 必需方法

    // 返回此浮动筛选器的 HTML 元素。
    getGui(): HTMLElement;

    /** 当列定义或模型更新时调用。 */
    refresh(params: FloatingFilterDisplayParams): void;

    // 可选方法

    // init(params) 方法在浮动筛选器上调用一次。
    // 有关参数的详细信息，请参见下文。
    init(params: FloatingFilterDisplayParams): void;

    // 每次显示弹出窗口时都会调用，在 getGui 返回的 GUI 附加到 DOM 之后。
    // 这对于任何需要在执行之前附加的逻辑很有用，例如将焦点放在特定的 DOM 元素上。
    afterGuiAttached(params?: IAfterGuiAttachedParams): void;

    // 当浮动筛选器被销毁时调用。像列标题一样，
    // 浮动筛选器的生命周期仅在列可见时，
    // 因此如果列变为不可见或用户通过水平滚动将列滚动出视图，它们将被销毁。
    destroy(): void;
}
```

### 自定义浮动筛选器参数

`init(params)` / `refresh(params)` 方法接受一个包含下面列出的项目的参数对象。如果通过 `colDef.floatingFilterComponentParams` 属性提供了自定义参数，这些参数将额外添加到参数对象中，如果存在名称冲突，将覆盖同名项目。
{% /if %}

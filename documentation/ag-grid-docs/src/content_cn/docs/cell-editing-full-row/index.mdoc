---
title: "全行编辑"
---

全行编辑适用于您希望行中的所有单元格同时变为可编辑的情况。这给用户的印象是正在编辑行所代表的记录。

要启用全行编辑，请设置表格选项 `editType = 'fullRow'`。

如果使用自定义单元格编辑器，单元格编辑器将以完全相同的方式工作，但有以下附加功能：

-   `focusIn`：如果您的单元格编辑器有 `focusIn()` 方法，当用户 Tab 进入单元格时会调用它。这应该用于将焦点放在要聚焦的特定项目上，例如单元格编辑器中的 `textfield`。
-   `focusOut`：如果您的单元格编辑器有 `focusOut()` 方法，当用户 Tab 离开单元格时会调用它。这没有预期的用途；它只是为了补充 `focusIn()` 方法。
-   事件：当行停止编辑时，对于值已更改的每列都会调用 `cellValueChanged` 事件，并且对于该行会调用一次 `rowValueChanged`。

### 全行编辑和弹出编辑器

全行编辑与弹出编辑器不兼容。这是因为 a) 表格同时为行中的每个单元格弹出编辑器会令人困惑，b) 导航和弹出的复杂性几乎无法建模，因此表格和您的应用程序代码会很混乱且非常容易出错。如果您使用全行编辑，则无法使用弹出编辑器。

这并不意味着您不能从"单元格内"编辑器显示弹出窗口 —— 您可以自由这样做 —— 但是显示和隐藏弹出窗口的责任属于您的编辑器。您可能希望使用表格的焦点事件在用户 Tab 或点击离开单元格时隐藏弹出窗口。

### 示例：全行编辑

下面的示例显示全行编辑。除了标准的全行编辑外，还应注意以下几点：

-   '价格'列有一个自定义编辑器，演示了您应该如何实现 `focusIn()` 方法。
    此编辑器的 `focusIn()` 和 `focusOut()` 都记录到控制台。请注意，`focusIn()`
    和 `focusOut()` 仅在编辑时用户在单元格之间 Tab 时调用，当用户双击单元格开始编辑该单元格时，或用户通过例如按 {% kbd "↵ Enter" /%} 键完成编辑该单元格时不会调用它们。
-   在编辑时按 {% kbd "⇥ Tab" /%} / {% kbd "⇧ Shift" /%} & {% kbd "⇥ Tab" /%} 将在编辑行的单元格之间移动焦点。当行处于编辑模式时，只读单元格将可获得焦点。
-   '禁止导航'列无法使用 {% kbd "⇥ Tab" /%} / {% kbd "⇧ Shift" /%} & {% kbd "⇥ Tab" /%} 导航。换句话说，当在表格周围 Tab 时，您无法 Tab 到此单元格。
-   **只读**列不可编辑，因此当行进入编辑模式时，**只读**列中的单元格无法编辑。
-   按钮将开始编辑第二行。它使用 API 开始编辑单元格，但结果是整行将变为可编辑，从指定的单元格开始。
-   `cellValueChanged` 和 `rowValueChanged` 事件记录到控制台。

{% gridExampleRunner title="全行编辑" name="full-row-editing"  exampleHeight=527 /%}

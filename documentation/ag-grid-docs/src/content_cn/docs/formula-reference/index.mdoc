---
title: "公式参考"
enterprise: true
---

本页列出了所有支持的操作符和内置函数以供快速参考。有关语法用法和示例，请参阅主[公式](./formulas/)页面。

## 数学运算符

| 符号 | 描述                                                                         |
| ------ | ----------------------------------------------------------------------------------- |
| `+`    | 加法，可用于将两个数字相加，或将天数添加到日期                     |
| `-`    | 减法，可用于将两个数字相减，或从日期中减去天数      |
| `*`    | 乘法，可用于将两个数字相乘                                 |
| `/`    | 除法，可用于将两个数字相除                                         |
| `^`    | 幂运算，可用于将数字提升到幂                            |
| `&`    | 连接，可用于连接两个字符串                                      |
| `=`    | 等于，可用于比较两个值                                         |
| `<>`   | 不等于，可用于比较两个值                                     |
| `>`    | 大于，可用于比较两个值                                     |
| `<`    | 小于，可用于比较两个值                                        |
| `>=`   | 大于或等于，可用于比较两个值                         |
| `<=`   | 小于或等于，可用于比较两个值                            |
| `%`    | 百分比后缀运算符，将值除以 100（例如 `50%` 计算为 `0.5`） |

## 提供的函数

**数值函数**

| 函数                              | 描述                                                                                                                                                            |
| ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `SUM(arg1, arg2, ...)`                | 返回所有参数的总和。                                                                                                                                      |
| `SUMIF(range, criteria, [sum_range])` | 返回 `sum_range` 中满足 `criteria` 的对应 `range` 值的总和。如果未提供 `sum_range`，则使用 `range` 进行求和。 |
| `PRODUCT(arg1, arg2, ...)`            | 返回所有参数的乘积。                                                                                                                                  |
| `MIN(arg1, arg2, ...)`                | 返回参数中的最小值。                                                                                                                         |
| `MAX(arg1, arg2, ...)`                | 返回参数中的最大值。                                                                                                                         |
| `AVERAGE(arg1, arg2, ...)`            | 返回参数中所有数值的平均值。                                                                                                         |
| `MEDIAN(arg1, arg2, ...)`             | 返回参数中所有数值的中位数。                                                                                                          |
| `POWER(arg1, arg2)`                   | 返回将第一个参数提升到第二个参数幂的结果。                                                                                  |
| `RAND()`                              | 返回 0 到 1 之间的随机数。                                                                                                                               |

**日期函数**

| 函数  | 描述                                                                        |
| --------- | ---------------------------------------------------------------------------------- |
| `NOW()`   | 返回表示当前日期和时间的日期对象。                      |
| `TODAY()` | 返回表示当前日期且时间设置为 00:00:00 的日期对象。 |

**文本函数**

| 函数                  | 描述                                      |
| ------------------------- | ------------------------------------------------ |
| `CONCAT(arg1, arg2, ...)` | 将所有参数连接成单个字符串。 |

**逻辑函数**

| 函数                                       | 描述                                                                           |
| ---------------------------------------------- | ------------------------------------------------------------------------------------- |
| `IF(condition, value_if_true, value_if_false)` | 如果条件为真，则返回 `value_if_true`，否则返回 `value_if_false`。 |

**计数函数**

| 函数                      | 描述                                                      |
| ----------------------------- | ---------------------------------------------------------------- |
| `COUNT(arg1, arg2, ...)`      | 返回参数中数值的计数。         |
| `COUNTA(arg1, arg2, ...)`     | 返回参数中非空值的计数。       |
| `COUNTBLANK(arg1, arg2, ...)` | 返回参数中空值的计数。           |
| `COUNTIF(range, criteria)`    | 返回范围中满足条件的值的计数。 |

## 错误代码

| 错误       | 描述                                                                        |
| ----------- | ---------------------------------------------------------------------------------- |
| `#REF!`     | 公式包含无效的单元格引用                                            |
| `#NAME?`    | 公式包含无效的操作                                                 |
| `#CIRCREF!` | 公式包含循环引用                                                |
| `#PARSE!`   | 无法解析公式值                                                      |
| `#VALUE!`   | 公式包含错误类型的值（例如函数的非数值参数） |
| `#DIV/0!`   | 公式导致除以零                                              |
| `#ERROR!`   | 公式包含其他类型的错误                                          |

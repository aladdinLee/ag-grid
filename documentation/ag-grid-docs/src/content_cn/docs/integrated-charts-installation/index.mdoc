---
title: "安装集成图表"
enterprise: true
---

本节展示如何安装集成图表。

{% if isFramework("react") %}
集成企业图表模块（包括 AG Charts Enterprise）可以通过 `AgGridProvider` 提供，如下所示：

```jsx
import { ClientSideRowModelModule } from 'ag-grid-community';
import { IntegratedChartsModule } from 'ag-grid-enterprise';
import { AgChartsEnterpriseModule } from 'ag-charts-enterprise';
import { AgGridProvider, AgGridReact } from 'ag-grid-react';

const modules = [
    ClientSideRowModelModule,
    IntegratedChartsModule.with(AgChartsEnterpriseModule),
];

function App() {
    return (
        <AgGridProvider modules={modules}>
            <AgGridReact /* ... props */ />
        </AgGridProvider>
    );
}
```

{% /if %}

{% if isFramework("javascript", "angular", "vue") %}
集成企业图表模块（包括 AG Charts Enterprise）可以按如下方式导入：

```ts
// 导入图表所需的最小模块
import { ModuleRegistry, ClientSideRowModelModule } from "ag-grid-community";
import { IntegratedChartsModule } from "ag-grid-enterprise";
import { AgChartsEnterpriseModule } from 'ag-charts-enterprise';

ModuleRegistry.registerModules([
    ClientSideRowModelModule,
    IntegratedChartsModule.with(AgChartsEnterpriseModule)
]);
```

{% /if %}

{% note %}
AG Charts Community（`AgChartsCommunityModule`）可从 `ag-charts-community` 包获得。
{% /note %}

{% if isFramework("javascript") %}

## CDN 安装

要通过 CDN 使用集成图表，您必须同时包含 AG Grid Enterprise 和 AG Charts Community 或 Enterprise 之一，如下所示。无需注册模块，因为这已在 UMD 文件中自动完成。

要通过 CDN 使用 AG Charts Community，请在 HTML 文件中包含以下脚本标签：

```html
<script src="https://cdn.jsdelivr.net/npm/ag-grid-enterprise@{% $agGridVersion %}/dist/ag-grid-enterprise.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/ag-charts-community@{% $agChartsVersion %}/dist/umd/ag-charts-community.min.js"></script>
```

要使用 AG Charts Enterprise，请在 HTML 文件中包含以下脚本标签：

```html
<script src="https://cdn.jsdelivr.net/npm/ag-grid-enterprise@{% $agGridVersion %}/dist/ag-grid-enterprise.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/ag-charts-enterprise@{% $agChartsVersion %}/dist/umd/ag-charts-enterprise.min.js"></script>
```

{% /if %}

---
title: "行动画"
---

行动画在过滤、排序、调整高度和展开/折叠行组后发生，默认启用。

## 禁用动画

根据网格的使用方式，行动画可能是性能瓶颈。
可以通过设置网格选项 `animateRows = false` 来禁用行动画。

## 行动画

网格将在以下场景中动画化行：

- 列动画：
  - 移动列
- 行动画
  - 过滤行
  - 排序行
  - 展开/折叠行组

下面的示例自动触发网格操作以演示动画。

{% gridExampleRunner title="动画" name="animation" /%}

## 动画序列详细信息

您不需要知道动画如何工作，但是，如果您正在创建主题或以其他方式想要调整动画，了解规则序列将很有用，如下所示：

- **新行：** 网格中的新行被放置在新位置并淡入。
- **旧行：** 不再在网格中的行保留在同一位置并淡出。
- **移动的行：** 处于新位置的行将其位置过渡到新位置。
- **调整高度的行：** 高度更改的行将高度过渡到新高度。

除了过渡动画外，旧行被放置在新行后面，以便移动的行在移动时位于旧行之上（因此旧行不会在新行之上淡出，而是在新行后面）。

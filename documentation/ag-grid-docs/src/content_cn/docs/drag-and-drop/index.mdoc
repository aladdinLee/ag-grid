---
title: "拖放"
---

拖放涉及使用操作系统的拖放支持在应用程序之间或应用程序内移动数据。使用拖放时，数据以类似于使用剪贴板的方式使用 MIME 类型移动或复制。

本机拖放通常用于在应用程序之间移动数据，例如将 URL 从电子邮件移动到 Web 浏览器以打开 URL，或将文件从文件浏览器应用程序移动到文本编辑器应用程序。本机拖放通常不用于在应用程序内操作数据。本机拖放类似于剪贴板功能，例如数据必须表示为 MIME 类型，对象不能通过引用传递（数据必须转换为 MIME 类型并复制）。

本节概述了网格如何适应本机操作系统拖放。假设读者熟悉拖放的工作原理。如果不熟悉，请参阅以下介绍之一：

-   [W3C Schools](https://www.w3schools.com/html/html5_draganddrop.asp)
-   [MDN Web Docs](https://developer.mozilla.org/en-US/docs/Web/API/HTML_Drag_and_Drop_API)

{% note %}
当您需要将网格数据导出到外部应用程序或
需要使用浏览器拖动事件时，应使用此功能，因为无法知道内容可能被放置在哪里。
对于所有基本场景，例如在同一页面中的元素之间拖动数据或网格到网格，
网格实现了自己的拖放，独立于操作系统的拖放。
它在网格内部用于[行拖动](./row-dragging/)（用于重新排序行）和列拖动
（例如重新排序列或在[列工具面板](./tool-panel-columns/)中移动列）。
网格在这些情况下使用自己的实现，因为它需要对数据进行比本机浏览器拖放支持的更精细的控制。例如，本机拖放不提供
对拖动项目的访问，直到拖动操作完成。
{% /note %}

## 启用拖动源

要允许从网格拖动，请在其中一列上设置属性 `dndSource=true`。
这将导致该列显示拖动手柄。当拖动开始时，网格
将默认创建数据的 JSON 表示，并将其设置为 MIME 类型 `application/json` 和 `text/plain`。

{% apiDocumentation source="column-properties/properties.json" section="row dragging" names=["dndSource"] /%}

在下面的示例中，请注意以下内容：

-   第一列有 `dndSource=true`，因此在第一列的单元格上开始鼠标拖动将开始拖动操作。

-   当数据被拖动到放置区域时，放置区域将显示接收到的 JSON。这是因为放置区域被编程为接受 `application/json` MIME 类型。

-   您还可以拖动到浏览器之外的其他应用程序。例如，某些文本编辑器（例如 Sublime Text）或文字处理器（例如 Microsoft Word）将基于 `text/plain` MIME 类型接受拖动。您可以通过将单元格拖动到例如 Microsoft Word 来测试这一点。

{% gridExampleRunner title="简单" name="simple" /%}

## 在网格之间拖动

可以在 AG Grid 的两个实例之间拖动行。拖动的方式与上述简单情况完全相同。放置的方式如下面的示例所示。

在下面的示例中，请注意以下内容：

-   行可以从一个网格拖动到另一个网格。当接收到行时，它**不会**从第一个网格中删除。这是示例的选择。示例同样可以从另一个网格中删除。

-   如果行已经存在于网格中，则不会添加两次。这也是示例的选择。

-   可以通过将行拖动到"Trash"放置区域来从两个网格中删除行。

-   可以通过从红色、绿色和蓝色"Create"可拖动区域拖出来创建新行。

{% gridExampleRunner title="两个网格" name="two-grids" /%}

请注意，网格中没有特定的放置区域逻辑。这是经过分析后故意这样做的。

在初步分析中，考虑在网格中为放置区域相关事件公开回调或触发事件，例如 `onDragEnter`、`onDragExit` 等。但是，考虑到开发人员可以直接向网格 div 添加此类事件监听器，这并没有增加任何额外的价值。

鉴于网格只是公开底层事件/回调，而不是自己进行任何处理，它不会增加任何价值，因此提供此类回调只会添加一层无用的逻辑。

## 自定义拖动数据

数据的 JSON 表示可能不是所需的拖动数据。

要提供替代拖动数据，请在列定义上使用 `dndSourceOnRowDrag` 回调。这允许应用程序对 `rowdrag` 事件进行特定处理，而不是默认的网格行为。

{% apiDocumentation source="column-properties/properties.json" section="row dragging" names=["dndSourceOnRowDrag"] /%}

下面的示例与本页上的第一个示例相同，只是添加了自定义拖动数据。请注意以下内容：

-   可拖动列还设置了 `dndSourceOnRowDrag`。
-   `onRowDrag` 方法提供要设置到拖动事件中的替代拖动数据。
-   拖动的数据还包括行状态，例如行是否被选中。

{% gridExampleRunner title="自定义拖动数据" name="custom-drag-data" /%}

## 自定义拖动组件

拖放是一个复杂的应用程序级需求。因此，组件（网格）无法提供适合所有应用程序的拖放解决方案。出于这个原因，如果应用程序有拖放需求，您可能希望为您的需求专门实现自定义[单元格渲染器](./component-cell-renderer/)。

下面的示例显示了一个自定义拖放单元格渲染器。请注意以下内容：

-   拖动的工作方式与以前类似，行从左侧网格拖动到右侧放置区域。
-   网格不提供拖动。相反，示例的单元格渲染器实现了拖动逻辑。

{% gridExampleRunner title="自定义拖动组件" name="custom-drag-comp" /%}

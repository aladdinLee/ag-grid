{% if isFramework("vue") %}

```ts
interface IHeader {
    // 当为此标题设置了新的列定义时调用。
    // 如果您处理标题的刷新，则返回 true，否则返回 false，
    // 表格将从头重新创建您的标题。
    refresh(params: IHeaderParams): boolean;
}
```

{% /if %}

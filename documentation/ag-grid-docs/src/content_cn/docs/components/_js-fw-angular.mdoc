{% if isFramework("angular") %}

## 组件生命周期钩子 agInit

{% /if %}

{% if isFramework("angular") %}
每个自定义 Angular 组件都必须实现 `agInit(params)` 生命周期钩子。AgInit 由 AG Grid 在任何 [Angular 生命周期钩子](https://angular.dev/guide/components/lifecycle#)之前调用，包括 `ngOnInit`。此顺序是确定性的，适用于所有组件类型。
{% /if %}

{% if isFramework("angular") %}

## 混合使用 JavaScript 和 Angular

提供自定义组件时，您可以选择以下方式：

1. 将 AG Grid 组件作为 Angular 组件提供。
1. 将 AG Grid 组件作为 JavaScript 提供。

以下代码片段展示了如何同时使用 JavaScript 和 Angular 组件：

{% /if %}

{% if isFramework("angular") %}

```ts
//...其他导入
import { Component } from '@angular/core';
import JavascriptComponent from './JavascriptComponent.js';
import { AngularComponent }  from './angular.component';

@Component({
selector: 'app-root',
template: `
  <ag-grid-angular [columnDefs]="columnDefs" [components]="components"
                   ...其他属性 />
`
})
export class AppComponent {
  // JS 和 Angular 组件，仅当按名称查找时才需要注册
  components = {
      'javascriptComponent': JavascriptComponent,
      'angularComponent': AngularComponent
  };
  columnDefs = [
      {
          headerName: "JS Cell",
          field: "value",
          cellRenderer: 'javascriptComponent', // JS 组件按名称
      },
      {
          headerName: "JS Cell",
          field: "value",
          cellRenderer: JavascriptComponent, // JS 组件按直接引用
      },
      {
          headerName: "Angular Cell",
          field: "value",
          cellRenderer: 'angularComponent', // Angular 组件按名称
      },
      {
          headerName: "Angular Cell",
          field: "value",
          cellRenderer: AngularComponent, // Angular 组件按直接引用
      }
  ];

  //...其他属性和方法
}
```

{% /if %}

{% if isFramework("angular") %}
{% warning %}
JavaScript 组件在 NgZone 之外运行。如果它们发起对 Angular 应用程序的调用，您可能需要将这些调用包装在 `ngZone.run()` 中，以便变更检测能够正确运行。
{% /warning %}
{% /if %}

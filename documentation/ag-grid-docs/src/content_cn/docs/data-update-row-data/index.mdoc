---
title: "更新行数据"
description: "通过更新 rowData 网格属性在 $framework 数据网格中更新行数据。提供行 ID 以在行数据更改之间维护网格选择"
---

通过更新 `rowData` 网格属性来更新网格内的行数据。

下面的示例显示了具有两组数据的网格。单击按钮在数据集之间切换或清除行数据。某些行在数据集之间是通用的，但是如果选择了任何行（通过单击行），则选择在行更新之间丢失，因为未提供行 ID。

{% gridExampleRunner title="简单行数据" name="simple-row-data" /%}

下面的示例与上面的示例相同，除了通过 `getRowId()` 回调提供[行 ID](./row-ids/)。这导致在行数据更改之间维护[行选择](./row-selection/)（假设行存在于两个集合中），并且不会从头开始重绘 HTML，从而产生[行动画](./row-animation/)。

{% gridExampleRunner title="简单行 ID" name="simple-row-id" /%}

提供[行 ID](./row-ids/) 允许网格在以下几个区域中以最佳方式工作，如下所示：

| 功能                                                  | 缺少行 ID                                                  | 提供行 ID                             |
| --------------------------------------------------------- | ---------------------------------------------------------------- | -------------------------------------------- |
| [行选择](./row-selection/)                         | 行选择丢失                                               | 行选择保持                     |
| [行分组](./grouping/)                               | 行组重新创建，所有打开的组关闭                    | 组保持/更新，打开的组保持打开 |
| [行刷新](./view-refresh/)                            | 所有行从 DOM 中销毁并重新创建，可能会出现闪烁 | 只有更改的行在 DOM 中更新     |
| [行动画](./row-animation/)                         | 没有行动画                                                 | 移动的行动画到新位置           |
| [闪烁单元格](./change-cell-renderers/#flashing-cells) | 没有闪烁可用，所有单元格从头创建        | 更改的值可以闪烁以显示更改 |

## 控制行位置

下面的示例演示了通过更新行数据来控制网格行，包括它们的顺序。

该示例保留记录列表以模仿"存储"中的数据。每次用户进行更新时，存储中的数据都会被复制，以便当向网格提供行数据时，网格会呈现不同的行数据。这相当于从服务器刷新数据，或在客户端上使用不可变数据存储。

请注意以下内容：

-   **Reverse**：反转项目的顺序。行被移动而不是重新创建。没有闪烁。

-   **Append Items**：在末尾添加五个项目。行被移动而不是重新创建。没有闪烁。

-   **Prepend Items**：在开头添加五个项目。没有闪烁。

-   请注意，如果应用了网格排序，则网格排序顺序优先于提供的列表中数据的顺序。

-   **Remove Selected**：删除选定的项目。尝试选择多行（使用复选框，{% kbd "⇧ Shift" /%} + 单击以选择范围）并同时删除多行。注意其余行如何动画到新位置。

-   **Update Prices**：更新所有价格。尝试按价格排序，并注意随着价格变化顺序的变化。还可以尝试突出显示价格范围，并在状态栏中查看聚合。当您更新价格时，聚合值会重新计算。

-   **Turn Grouping On / Off**：打开和关闭按符号分组。

-   **Group Selected A / B / C**：在分组打开的情况下，点击按钮 Move to Group A、B 和 C 将选定的项目移动到该组。注意行如何动画到新位置。

（注意：该示例使用仅限企业的功能[行分组](./grouping/)、[单元格选择](./cell-selection/)和[状态栏](./status-bar/)。）

{% gridExampleRunner title="简单不可变存储" name="simple-immutable-store"  exampleHeight=540 /%}

## 工作原理

当提供行 ID 时，网格假设它从不可变存储中获取数据，其中以下内容对数据成立：

-   对单个行数据项的更改会导致新的行数据项对象实例。
-   列表或行数据中的任何更改都会导致新列表。

网格使用以下规则计算需要将哪些更改应用到网格：

-   如果新项目的 ID 在网格中没有对应的项目，则将其作为新行添加到网格中。

-   如果新项目的 ID 在网格中有对应的项目，则比较对象引用。如果对象引用不同，则使用新数据更新行，否则假定数据与已存在的数据相同。

-   如果网格中的项目在新数据中没有对应的项目，则删除这些行。

-   最后，网格中的行被排序以匹配新提供的列表中的顺序。

## 示例：不可变存储 - 大数据集

下面是一个具有超过 11,000 行的数据集，在多列上具有行分组和聚合。就客户端行数据而言，这是一个相当复杂的网格。从示例中，请注意以下内容：

-   使用回调 `getRowId()` 提供行 ID。
-   选择 Update 按钮更新一系列数据。
-   请注意，应用更新后所有网格状态（行和范围选择、过滤器、排序等）都保持不变。

{% gridExampleRunner title="复杂不可变存储" name="complex-immutable-store"  exampleHeight=590 /%}

## 与事务更新的比较

当设置行数据而不提供行 ID 时，网格会将所有数据从网格中删除，并使用新的行数据从头开始。

但是，当提供行 ID 并更新行数据时，网格会在底层创建一个[事务更新](./data-update-transactions/)。换句话说，一旦网格计算出添加、更新和删除了哪些行，它就会创建一个包含这些详细信息的事务并应用它。这意味着事务更新的所有操作优势同样适用于设置行数据并提供行 ID。

但是，更新行数据（带有行 ID）和事务更新之间存在一些差异。这些差异如下：

-   当设置行数据时，网格将承担识别添加、删除和更新哪些行的开销。

-   当设置行数据时，网格按照提供数据的相同顺序存储数据。例如，如果您提供一个在列表中间添加数据的新列表，网格也会将数据放入列表的中间，而不是仅仅附加到末尾。这决定了没有应用网格排序时的数据顺序。如果您的应用程序不需要这样做，那么您可以通过在[网格选项](./grid-options/#reference-sort-suppressMaintainUnsortedOrder)中设置 `suppressMaintainUnsortedOrder=true` 来抑制此行为以提高性能。

-   在更新行数据时，没有[异步事务](./data-update-high-frequency/)的等效项。如果您想要一个管理高频数据更改的网格，不要直接更新行数据，而是使用[异步事务](./data-update-high-frequency/)。

由于上述原因，如果您有大型数据集（数千行）并且正在寻找加快速度的方法，请考虑使用[事务更新](./data-update-transactions/)。

如果您有较小的数据集（数百行），那么一切应该都可以正常工作，没有明显的延迟。

{% if isFramework("vue") %}

## 双向绑定

默认情况下，`:rowData` 是一个**单向绑定**：数据流入网格，但在网格内进行的更改（例如通过单元格编辑）**不会**传播回父组件的 `rowData` 变量。

要使行数据更改从网格流回到父组件，请使用 `v-model` 而不是 `:rowData`。

例如：

```jsx
<template>
    <ag-grid-vue style="width: 500px; height: 500px;"
                 @grid-ready="onGridReady"
                 :columnDefs="columnDefs"
                 v-model="rowData">
    </ag-grid-vue>
</template>
```

{% note %}
`v-model` 仅在使用 `ClientSideRowModel` 时可用，并且注册了 `AllCommunityModule` 或 `ClientSideRowModelApiModule` 模块。
{% /note %}
{% /if %}

## 添加新行

最终用户添加其他数据行不受内置支持。但是，可以使用自定义代码实现所需的行为。下面的示例显示了使用[全行编辑](./cell-editing-full-row/)和[行固定](./row-pinning/)实现此目的的一种方法。

{% gridExampleRunner title="向不可变存储添加行" name="add-rows-to-immutable-store"  exampleHeight=540 /%}

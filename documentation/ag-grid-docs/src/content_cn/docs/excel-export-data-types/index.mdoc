---
title: "Excel 导出 - 数据类型"
enterprise: true
---

Excel 导出器允许您将值导出为不同的 Excel 数据类型。

## 字符串、数字和布尔值

为了在导出的 Excel 文件中正确显示单元格值，您需要设置在 Excel 导出过程中使用的适当格式。在下面的部分中，我们演示了不同的值格式化，以将值导出为不同的 Excel 数据类型。

请注意：

-   我们在 `excelStyles` 数组中定义要导出到的 Excel 类型/格式列表。这些样式包括**唯一 id**，以及 `dataType` 或 `numberFormat`。

-   在网格列定义中，我们链接到 `excelStyles` 数组中定义的相应类型，存储我们要为列值应用的导出配置。

```{% frameworkTransform=true %}
const gridOptions = {
    columnDefs: [
        { headerName: 'provided', field: 'rawValue' },
        { headerName: 'number', field: 'rawValue', cellClass: 'numberType' },
        { headerName: 'currency', field: 'rawValue', cellClass: 'currencyFormat' },
        { headerName: 'boolean', field: 'rawValue', cellClass: 'booleanType' },
        { headerName: 'Negative', field: 'negativeValue', cellClass: 'negativeInBrackets' },
        { headerName: 'string', field: 'rawValue', cellClass: 'stringType' },
        { headerName: 'Date', field: 'dateValue', cellClass: 'dateType', minWidth: 220 },
    ],
    rowData: [
        {
            rawValue: 1,
            negativeValue: -10,
            dateValue: '2009-04-20T00:00:00.000',
        },
    ],
    excelStyles: [
        {
            id: 'numberType',
            numberFormat: {
                format: '0',
            },
        },
        {
            id: 'currencyFormat',
            numberFormat: {
                format: '#,##0.00 €',
            },
        },
        {
            id: 'negativeInBrackets',
            numberFormat: {
                format: '$[blue] #,##0;$ [red](#,##0)',
            },
        },
        {
            id: 'booleanType',
            dataType: 'Boolean',
        },
        {
            id: 'stringType',
            dataType: 'String',
        },
        {
            id: 'dateType',
            dataType: 'DateTime',
        },
    ],
    popupParent: document.body
};
```

以下示例演示了如何为导出使用其他数据类型。

请注意：

-   布尔值通过使用 `1` 表示 `true`，`0` 表示 `false` 来工作。导出为布尔值时，所有其他值都会产生错误。
-   当您提供 `numberFormat` 时，值将使用提供的格式导出为数字。您可以设置小数位数，以不同方式格式化负值，并根据值更改导出值的颜色。
-   当使用 dataType: 'DateTime' 时，Excel 的日期时间格式为 `yyyy-mm-ddThh:MM:ss.mmm:`
-   如果您尝试导出与基础数据类型不兼容的值，Excel 将在打开文件时显示错误。
-   当使用 `dataType: 'DateTime'` 时，Excel 不会格式化结果值，在此示例中它显示 `39923`。您需要在 Excel 内部添加格式。您可以在下面的[日期](./excel-export-data-types/#dates)部分中看到有关如何处理日期格式的更好示例。

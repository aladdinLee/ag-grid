---
title: "网格布局"
description: "为您的 $framework 表格格式化网格布局。更改网格宽度/高度、分配 DOM 布局值、格式化动态调整大小。"
---

在正常使用下，您的应用程序应该使用 CSS 样式设置网格的宽度和高度。然后网格将适应您提供的宽度，并在网格内使用滚动以允许查看所有行和列。

{% if isFramework("javascript") %}

```html
<!-- 使用百分比设置宽度 -->
<div id="myGrid" style="width: 100%; height: 100%;"></div>

<!-- 或使用固定像素设置宽度 -->
<div id="myGrid" style="width: 500px; height: 200px;"></div>
```

{% /if %}

{% if isFramework("angular") %}

```html
<!-- 使用百分比设置宽度 -->
<ag-grid-angular style="width: 100%; height: 100%;" />

<!-- 或使用固定像素设置宽度 -->
<ag-grid-angular style="width: 500px; height: 200px" />
```

{% /if %}

{% if isFramework("react") %}

```html
<!-- 使用百分比设置宽度 -->
<AgGridReact style={{ width: '100%', height: '100%' }} />

<!-- 或使用固定像素设置宽度 -->
<AgGridReact style={{ width: 500, height: 200 }} />
```

{% /if %}

{% if isFramework("vue") %}

```html
<!-- 使用百分比设置宽度 -->
<ag-grid-vue style="width: 100%; height: 100%;"></ag-grid-vue>

<!-- 或使用固定像素设置宽度 -->
<ag-grid-vue style="width: 500px; height: 200px"></ag-grid-vue>
```

{% /if %}

{% warning %}
如果使用 % 作为高度，请确保放置网格的容器也指定了高度，因为浏览器将根据父级高度的百分比调整 div，如果父级没有高度，则此 % 将始终为零。

如果网格的大小不是您认为应该的大小，则在网格的 div 上放置边框，看看是否是您想要的大小（网格将填充此 div）。如果不是您想要的大小，则您的应用程序中存在 CSS 布局问题。
{% /warning %}

## DOM 布局

网格可以有三个 DOM 布局值：'normal'、'autoHeight' 和 'print'。它们的使用方式如下：

-   **normal**：如果未指定任何内容，则这是默认值。网格适应您提供的 div 的宽度和高度，并在两个方向上滚动。
-   **autoHeight**：网格的高度设置为适应行数，因此网格不提供垂直滚动条。网格正常水平滚动。请注意，如果将其与 SSRM 一起使用，网格将尝试加载每一行，并可能导致不良副作用（例如过多的数据源请求或加载的行过多）。
-   **print**：不使用滚动条，网格渲染所有行和列。此布局在[打印](./printing/)中说明。

## 正常布局

如果网格初始化后宽度和/或高度发生变化，网格将自动调整大小以填充新区域。

下面的示例显示设置网格大小，然后在用户选择按钮时更改它。

{% gridExampleRunner title="宽度和高度" name="width-and-height" /%}

### 无水平滚动的动态调整大小

有时您希望让不适合当前视口的列完全隐藏，而不显示水平滚动条。

要实现这一点，请确定网格的宽度并计算出可以容纳在该空间中的列数，隐藏任何不适合的列，根据 `gridSizeChanged` 事件触发不断更新，如下一个示例所示。

最好在新标签页中打开此示例 - 然后更改浏览器的水平大小，观察列如何根据当前网格大小隐藏/显示。

{% gridExampleRunner title="无滚动的动态水平调整大小" name="example1" /%}

### 动态垂直调整大小

有时网格的垂直高度大于其中的行数。您可以动态设置行高以填充可用高度，如下例所示：

{% gridExampleRunner title="动态垂直调整大小" name="example2" /%}

## 自动高度布局

根据您的场景，您可能希望网格自动调整其高度以适应网格内显示的行数。如果您有相对较少的行，并且不希望在最后一行和网格底部之间有空空间，这将非常有用。

要允许网格自动调整其高度以适应行，请设置网格属性 `domLayout='autoHeight'`。

当 `domLayout='autoHeight'` 时，您的应用程序**不应**在网格 div 上设置高度，因为应该允许 div 自然流动以适应网格内容。当自动高度关闭时，您的应用程序**应该**在网格 div 上设置高度，因为网格将填充您提供的 div。

{% warning %}
如果使用网格自动高度，则网格将所有行渲染到 DOM 中。这与正常操作不同，在正常操作中，网格仅渲染在网格可滚动视口内可见的行。对于大型网格（例如 >1,000 行），网格的绘制时间会很慢，或者对于非常大的网格，您的应用程序可能会冻结。这不是网格的问题，而是浏览器在一个网页上可以轻松显示多少数据的限制。因此，如果显示大量数据，不建议使用网格自动高度。而是正常使用网格，网格的行虚拟化将为您解决此问题。
{% /warning %}

下面的示例演示了 autoHeight 功能。请注意以下几点：

-   当您在网格中设置不同数量的行时，网格将调整其高度以适应行。
-   当网格高度超过浏览器高度时，您需要使用浏览器垂直滚动查看数据（如果您正在查看下面嵌入的示例，则使用 iFrames 滚动）。
-   高度也会在筛选时调整，以添加和删除行。
-   如果您有固定行，网格将调整大小以容纳固定行。
-   不会发生垂直滚动，但水平滚动（包括固定列）将正常工作。
-   可以使用 `api.setGridOption('domLayout', layout)` 或通过更改绑定属性 `domLayout` 将网格移入和移出"全高"模式。

{% note %}
如果您在新标签页中打开以下测试，则最好查看，因此很明显没有滚动条。
请注意，如果您使用内联示例，显示的滚动条是包含 `iframe` 的，而不是网格。
{% /note %}

{% gridExampleRunner title="自动高度" name="auto-height"  exampleHeight=580 /%}

### 自动高度的最小高度

使用自动高度时，网格行部分设置了 150px 的最小值。这是为了避免空网格看起来很奇怪。要删除此最小高度，请添加以下 CSS：

```css
.ag-center-cols-viewport {
    min-height: unset !important;
}
```

使用自动高度时无法指定最大高度。

{% note %}
用户询问使用自动高度时是否可以设置最大高度？答案是否定的。
如果使用自动高度，网格设置为以不同的方式工作。无法切换。

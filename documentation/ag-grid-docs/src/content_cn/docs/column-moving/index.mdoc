---
title: "列移动"
---

列可以在网格中以以下方式移动：

-   使用鼠标或通过触摸拖动列标题。
-   使用[键盘](#move-via-keyboard)移动聚焦的列标题。
-   使用[网格 API](#move-via-api)。

## 简单示例

下面的示例演示了通过鼠标拖动和 API 进行简单移动。可以注意到以下几点：

-   使用鼠标拖动列标题会将列移动到新位置。
-   **Medals First** 和 **Medals Last** 按钮调用 API `moveColumns(keys, toIndex)` 将奖牌列分别放置在开头或结尾。
-   **Country First** 按钮调用 API `moveColumns([key], toIndex)` 将 Country 列放在第一位。
-   **Swap First Two** 按钮调用 API `moveColumnByIndex(fromIndex, toIndex)` 交换前两列。
-   聚焦列标题并按 {% kbd "⇧ Shift" /%} + {% kbd "←" /%} / {% kbd "→" /%} 会在该方向移动列。
-   **Print Columns** 按钮调用 API `getAllGridColumns()` 将当前列顺序打印到开发者控制台。

{% gridExampleRunner title="Column Moving Simple" name="moving-simple" /%}

## 通过键盘移动

可以使用键盘移动列标题。当列标题聚焦时，按 {% kbd "⇧ Shift" /%} + {% kbd "←" /%} / {% kbd "→" /%} 在该方向移动列。网格将自动滚动以保持移动的列可见。

有关标题键盘交互的完整列表，请参阅[列标题导航](./keyboard-navigation/#column-header-navigation)。

## 通过 API 移动

用于移动列的网格 API 方法如下：

{% apiDocumentation source="grid-api/api.json" section="columnMoving" names=["moveColumns", "moveColumnByIndex"] /%}

## 移动动画

移动列时会发生列动画。默认情况下动画是开启的。建议您保持列移动动画开启，除非您的目标平台（浏览器和硬件）太慢而无法管理动画。要关闭列动画，请设置网格属性 `suppressColumnMoveAnimation=true`。

{% video videoSrc="resources/column-animation.mp4" centered=true /%}

移动列动画仅过渡列的位置，因此当您移动列时，它会动画到新位置。除位置外，没有其他属性被动画化。

## 抑制隐藏离开

网格属性 `suppressDragLeaveHidesColumns` 将阻止列在拖到网格外部时从网格中移除。如果用户在移动列时不小心将列移到网格外部，但不打算将其隐藏，这很方便。

## 拖动时抑制移动

默认情况下，列在拖动时会移动。由于您的应用程序设计，此效果可能不理想。要防止这种情况，请在 `gridOptions` 中使用 `suppressMoveWhenColumnDragging`。

```{% frameworkTransform=true %}
const gridOptions = {
    suppressMoveWhenColumnDragging: true
}
```

{% gridExampleRunner title="Column Moving with SuppressMoveWhenColumnDragging" name="suppress-move-when-dragging" /%}

## 抑制可移动

列属性 `suppressMovable` 更改列是否可以拖动。当 `suppressMovable=true` 时，用户无法拖动列标题来移动列。但是，通过将其他列放置在其周围，该列可能会被无意中移动，因此只有在所有列都具有此属性时才实用。

## 锁定位置

列属性 `lockPosition` 将列锁定到网格的一侧。当 `lockPosition` 设置为 `"left"` 或 `"right"` 时，列将始终锁定到该位置，不能被用户拖动，也不能通过拖动其他列而移出位置。

## 抑制可移动和锁定位置示例

下面的示例演示了这些属性，如下所示：

-   **Age** 列锁定为 `"left"`，作为网格可滚动区域的第一列。无法移动此列，也无法让其他列移动到它上面以影响其位置。因此，**Age** 列标记可滚动区域的开始，无论其在列定义中的位置如何。
-   **Total** 列锁定为 `"right"`，同样其位置不会受到移动其他列的影响。
-   **Athlete** 列的移动被抑制。无法移动此列，但可以在其周围移动其他列。
-   网格设置了 `suppressDragLeaveHidesColumns` 为 `true`，因此拖到网格外部的列不会隐藏（通常将列拖出网格会隐藏该列）。
-   `defaultColDef` 设置了 `lockPinned` 为 `true`，因此无法将列固定到锁定列的左侧或右侧。
-   **Age**、**Total** 和 **Athlete** 列分别应用了用户提供的 `locked-col` 和 `suppress-movable-col` CSS 类来更改背景颜色。

{% gridExampleRunner title="Column Suppress & Lock" name="suppress-and-lock" /%}

## 高级锁定位置示例

下面是一个更真实的示例，说明锁定列的使用场景。第一列包含操作按钮，例如"删除"、"购买"、"出售"等。

从示例中可以注意到以下几点：

-   第一列通过设置 `colDef.lockPosition='left'` 锁定在第一位置。这意味着它不能被移出位置，其他列也不能在其周围移动。
-   第一列应用了用户提供的 `locked-col` CSS 类来更改背景颜色。
-   示例应用程序监听列固定事件。如果列被左固定，锁定列也会被左固定以保持它们在第一位置。右固定不会影响锁定列。
    -   单击 **Pin Athlete Left** 将左固定 Athlete 列，这将导致锁定列被固定。
    -   单击 **Pin Athlete Right** 将右固定 Athlete 列，这不会影响锁定列。
    -   单击 **Un-Pin Athlete** 将取消固定 Athlete 列，这将导致锁定列被取消固定（假设没有其他列被左固定）。

{% gridExampleRunner title="Advanced Lock" name="advanced-lock" /%}

## 锁定可见性

当您移动列时，可以按以下方式更改其可见性：

-   您可以通过将列拖到网格外部来隐藏列。
-   您可以通过从[工具面板](./tool-panel/)将列拖到网格上来显示列（当网格选项 `allowDragFromColumnsToolPanel=true` 时）。

列属性 `lockVisible` 将阻止单个列通过 UI 变为可见或隐藏。当 `lockVisible=true` 时，列在拖出网格时不会隐藏，从工具面板拖到网格上的列也不会变为可见。

与属性 `suppressDragLeaveHidesColumns` 略有重叠。当 `suppressDragLeaveHidesColumns=true` 时，所有列在拖到网格外部时都保持可见。这是阻止所有列在用户通过拖动重新排序列时隐藏的好方法。`lockVisible` 属性位于列级别，阻止所有更改列可见性的 UI 功能。

### 锁定可见性示例

下面的示例显示了锁定可见性。可以注意到以下几点：

-   启用了 `allowDragFromColumnsToolPanel`，以便可以通过从工具面板拖动来显示列。
-   列 **Age**、**Gold**、**Silver** 和 **Bronze** 都锁定了可见性。无法通过将它们拖出网格来隐藏这些列，也无法通过从工具面板拖入来显示这些列。
-   如果您在工具面板中使组可见或隐藏，锁定列不会受到影响。
-   如果您将组（例如 **Athlete** 组）拖出网格，组中的所有普通列都会被移除，组中的所有锁定列都保持不变。

{% gridExampleRunner title="Lock Visible" name="lock-visible"  exampleHeight=550 /%}

{% partial file="../shared-partials/_drag-and-drop-image-component.mdoc" /%}

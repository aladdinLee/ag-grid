---
title: "打印"
---

本节解释如何使用网格的打印布局功能。

使用打印布局的网格不会使用任何滚动条，因此所有行和列都会被打印。网格将自动调整宽度和高度以适应所有内容。这意味着如果网格打印在纸上，所有单元格都将被包含，而不是打印带有滚动条的网格，只有可见区域内的单元格会被打印。

下面的示例显示打印布局。可以注意以下几点：

- 按下按钮'Printer Friendly Layout'将网格转换为打印布局并删除所有滚动。
- 按下按钮'Normal Layout'将网格返回正常。

{% gridExampleRunner title="打印简单" name="for-print-simple" /%}

## 切换打印布局

可以通过设置属性 `domLayout='print'` 或调用网格 API `setGridOption('domLayout', 'print')` 来打开打印布局。同样，可以通过取消设置 `domLayout` 属性或调用网格 API `setGridOption('domLayout', null)` 将布局设置回正常。

```{% frameworkTransform=true %}
// 将网格布局设置为 'print'
api.setGridOption('domLayout', 'print');
// 将布局重置回正常
api.setGridOption('domLayout', undefined);
```

## 切换网格大小

网格宽度和高度将自动调整以适应所有单元格的内容。为此，应用程序**不**应在网格组件上设置宽度或高度。如果使用打印布局，请确保没有为网格设置宽度或高度。

本页面上的所有示例最初在网格上设置宽度和高度，然后在设置打印布局时删除宽度和高度。

## 分页符

在打印布局中，所有行都将具有 CSS 属性 `page-break-inside: avoid`，因此行不会跨页分割（例如，行的一半在一个打印页上，另一半在下一个打印页上）。

## 详细打印示例

下面显示了一个更详细的示例，并自动显示打印对话框。从示例中，可以注意以下几点：

- 当按下'Print'时，网格将执行以下操作：
  - 将网格设置为打印布局。
  - 从网格中删除高度和宽度设置，以允许网格自动调整大小。
  - 等待两秒钟（以允许浏览器使用新设置重绘），然后调出打印对话框。
  - 当打印对话框关闭时，将网格设置回正常。

- 第一列'ID'是固定的。在打印布局中，该列不是固定的，因为在没有水平滚动时固定没有意义。

- 数据已分组，组行跨越网格宽度。此组行在打印布局中正常包含。

{% gridExampleRunner title="打印复杂" name="for-print-complex" /%}

## 动画和重绘

当网格处于打印布局时，网格**不**对行使用绝对定位，而是使用正常流布局行。换句话说，通常网格使用精确像素定位放置每行 - 这使得行动画等事情成为可能，其中网格通过更改绝对像素位置移动行，并使用 CSS 过渡使行动画到新位置。这通常在过滤或排序后发生。在打印模式下，行按照它们在 dom 中出现的顺序布局 - 这使得分页符等事情成为可能，但消除了动画的可能性。

在打印布局中，网格将在行发生任何更改时重绘整个网格。如果只添加/删除一行，或应用过滤器或排序，则删除整个 DOM 并从上到下再次插入所有行。这使得打印布局最适合非交互式网格（即用于打印），因为您将失去动画，并且对网格的更改将很昂贵（网格在每次更改时重绘所有内容）。

## 不要打印大数据

如果要显示大量行或列，请不要使用此技术进行打印。这不是网格的问题，而是浏览器在一个网页中可以轻松显示多少数据的限制。如果尝试将大量数据渲染到网页中，网页将创建大量 DOM 元素，并且会减慢速度或简单地挂起浏览器。AG Grid 通过虚拟化行和列来解决此问题。但是，如果渲染整个网格，则无法虚拟化行或列。

如果要允许打印大型数据集，最好让用户导出到 CSV 或 Excel，然后从其他非基于 Web 的应用程序打印。

## 仅在打印时保持打印布局

当网格处于打印布局时，它将在不使用行虚拟化的情况下渲染所有单元格。这意味着网格将较慢，因为它正在渲染的 DOM 量。仅在实际要打印时使用打印布局。所有功能（过滤、排序、拖动列等）都将工作，但是如果数据集很大，性能将受到影响，并且会让您的用户感到沮丧。因此，最好仅在打印时保持打印布局，并在其他所有时间保持正常（或自动高度）布局。

## 行模型

打印布局唯一适用的[行模型](./row-models/)是默认的[客户端](./row-models/#client-side)行模型。它不适用于其他行模型（[无限](./infinite-scrolling/)、[服务器端](./server-side-model/)或[视口](./viewport/)）。这是因为网格将渲染整个数据集，这与其他延迟加载数据的行模型的理念相悖。

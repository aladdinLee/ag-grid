---
title: "浮动过滤器"
---

浮动过滤器是列表头下方的额外一行，用户可以在其中查看并可选择编辑与每列关联的过滤器。

通过在 `colDef` 上设置属性 `floatingFilter = true` 来激活浮动过滤器：

```{% frameworkTransform=true %}
const gridOptions = {
    columnDefs: [
        // 启用浮动过滤器的列定义
        {
            field: 'country',
            filter: true,
            floatingFilter: true
        }
    ]
}
```

要默认为所有列启用浮动过滤器，您应该在 `defaultColDef` 上设置 `floatingFilter`。然后，您可以通过在单个 `colDef` 上设置 `floatingFilter = false` 来按列禁用浮动过滤器。

浮动过滤器依赖于主列过滤器并与其协调。它们没有自己的状态，而是显示主过滤器的状态，如果它们可编辑，则在主过滤器上设置状态。因此，没有用于获取或设置浮动过滤器状态的 API。

每个浮动过滤器都接受一个参数，用于自动显示/隐藏将打开主过滤器的按钮。

要查看浮动过滤器的工作原理，请参阅[浮动过滤器组件](./component-floating-filter/)。

以下示例显示了浮动过滤器的以下功能：

-   文本过滤器：具有开箱即用的读/写浮动过滤器（Athlete 和 Sport 列）
-   集合过滤器：具有开箱即用的只读浮动过滤器（Country 列）
-   'Print Country' 按钮将国家过滤器模型打印到开发者控制台。
-   日期和数字过滤器：具有所有过滤器的开箱即用读/写浮动过滤器，除了切换到范围过滤时，浮动过滤器为只读（Age 和 Date 列）
-   `buttons` 包含 `'apply'` 的列要求用户在浮动过滤器上按 {% kbd "↵ Enter" /%} 才能使过滤器生效（Gold 列）。（**注意：**这不适用于浮动日期过滤器，它们总是在输入有效日期后立即应用。）
-   直接对主过滤器所做的更改会自动反映在浮动过滤器中（更改任何主过滤器）
-   用户可以配置何时显示/隐藏显示完整过滤器的按钮（Silver 和 Bronze 列）
-   Year 列有过滤器，但浮动过滤器已禁用
-   Total 列没有过滤器，因此也没有浮动过滤器

{% gridExampleRunner title="浮动过滤器" name="floating-filter"  exampleHeight=615 /%}

## 提供的浮动过滤器

网格提供的所有默认过滤器都提供自己的浮动过滤器实现。启用这些浮动过滤器所需要做的就是设置 `floatingFilter = true` 列属性。提供的浮动过滤器的功能如下：

| 过滤器 | 可编辑    | 描述                                                                                                         |
| ------ | --------- | ------------------------------------------------------------------------------------------------------------ |
| 文本   | 有时      | 提供文本输入字段以显示过滤器值，如果只读则提供只读标签。                                                     |
| 数字   | 有时      | 提供数字输入字段以显示过滤器值（除非使用[自定义数字支持](./filter-number/#custom-number-support)），如果只读则提供只读标签。 |
| 日期   | 有时      | 提供日期输入字段以显示过滤器值，如果只读则提供只读标签。                                                     |
| 集合   | 否        | 通过连接所有选定的值提供只读标签。                                                                           |

文本、数字和日期（简单过滤器）的浮动过滤器在过滤器具有一个条件和一个值时可编辑。如果浮动过滤器具有 a) 两个或更多条件或 b) 零（自定义选项）或两个（'inRange'）值，则浮动过滤器为只读。

下面的屏幕截图显示了提供的数字浮动过滤器可编辑和只读的示例场景。

-   **一个值和一个条件 - 可编辑**

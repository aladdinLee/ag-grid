---
title: 'AG Grid 模型上下文协议 (MCP) 服务器'
headings:
    - slug: 'overview'
      text: '概述'
      depth: 2
    - slug: 'architecture'
      text: '架构'
      depth: 2
    - slug: 'installation'
      text: '安装'
      depth: 2
    - slug: 'getting-started'
      text: '入门'
      depth: 2
    - slug: 'prompts'
      text: '提示'
      depth: 2
    - slug: 'create-a-new-grid'
      text: '创建新网格'
      depth: 3
    - slug: 'migrate-to-a-new-version'
      text: '迁移到新版本'
      depth: 3
    - slug: 'resources-docs-api--example-search'
      text: '资源（文档、API 和示例搜索）'
      depth: 2
    - slug: 'tools'
      text: '工具'
      depth: 2
    - slug: 'search-docs'
      text: '搜索文档'
      depth: 3
    - slug: 'detect-version'
      text: '检测版本'
      depth: 3
    - slug: 'set-version'
      text: '设置版本'
      depth: 3
    - slug: 'config'
      text: '配置'
      depth: 2
---

AG Grid 的模型上下文协议 (MCP) 服务器为 LLM 提供框架和版本特定的知识，以帮助开发人员集成和维护他们的 AG Grid 代码。

{% video videoSrc="resources/ag-mcp-demo.mp4" centered=true /%}

## 概述

AG MCP 服务器的核心功能是 LLM 优化的搜索工具，它将以压缩的 markdown 格式提供额外的上下文（例如版本特定的文档、示例和引用），最大限度地减少使用的上下文量。

更具体地说，AG MCP 服务器由[工具](#tools)、[提示](#prompts)和[资源](#resources-docs-api--example-search)组成：

- **工具** - 模式定义的接口，使 AI 模型能够执行操作，包括[搜索文档](#search-docs)和[检测和设置当前 AG Grid 版本](#detect-version)。
- **提示** - 预配置的操作，允许您执行常见操作，例如[创建新网格](#create-a-new-grid)或[迁移到更高版本](#migrate-to-a-new-version)。
- **资源** - 以压缩 markdown 格式的文档、示例和 API 定义/接口。

每当您的 LLM 需要有关如何实现 AG Grid 功能的更多信息时，这些组件都可用，也可以直接访问。

## 架构

在高级别上，AG MCP 服务器的工作方式如下：

1. 向 LLM 客户端提交提示，例如 Claude、Cursor、CoPilot、ChatGPT 等...
2. LLM 访问 `ag-mcp` 服务器并通过工具调用、预构建的提示或请求特定资源来请求额外的上下文。
3. `ag-mcp` 使用 LLM 优化的搜索工具以压缩的 markdown 返回版本特定的信息，例如文档、示例和 API 引用。
4. LLM 使用此额外的上下文提供更准确的响应。

{% imageCaption imagePath="resources/ag-mcp-architecture.png" alt="AG MCP 架构" constrained=true enableDarkModeFilter=true /%}

## 安装

要使用您的 LLM 客户端安装和使用 `ag-mcp`，请在添加 MCP 服务器的正常过程中提供 `npx ag-mcp` 命令。

{% tabs %}
{% tabItem id="install-cursor" label="Cursor" %}

单击[在 Cursor 中安装](https://cursor.com/en/install-mcp?name=ag-mcp&config=eyJjb21tYW5kIjoibnB4IGFnLW1jcCJ9)以打开 Cursor 并自动添加 `ag-mcp` 服务器

要手动安装，请在项目根目录中创建 `mcp.json` 文件并添加以下内容：

```json
{
  "mcpServers": {
    "ag-mcp": {
      "command": "npx",
      "args": ["ag-mcp"]
    }
  }
}
```

{% note %}
要了解更多信息，请参见 [Cursor MCP 文档](https://cursor.com/docs/context/mcp)
{% /note %}

{% /tabItem %}
{% tabItem id="install-vs-code" label="VS Code (CoPilot)" %}

要手动安装，请在工作区中创建 `.vscode/mcp.json` 文件，并添加以下内容：

```bash
"servers": {
  "ag-mcp": {
    "command": "npx",
    "args": [
      "ag-mcp"
    ],
    "type": "stdio"
  }
  // 其他 MCP 服务器...
},
```

{% note %}
要了解更多信息，请参见 [VS Code MCP 文档](https://code.visualstudio.com/docs/copilot/customization/mcp-servers)。
{% /note %}

{% /tabItem %}
{% tabItem id="install-claude" label="Claude" %}

要将 `ag-mcp` 添加到 Claude 代码，运行以下命令：

```bash
claude mcp add ag-mcp npx ag-mcp
```

{% note %}
要了解更多信息，请参见 [Claude MCP 文档](https://docs.claude.com/en/docs/claude-code/mcp)
{% /note %}

{% /tabItem %}
{% /tabs %}

## 入门

安装 MCP 后，您的 LLM 将根据您的提示自动利用 AG MCP 服务器访问额外的上下文。也可以直接访问[工具](#tools)、[提示](#prompts)和[资源](#resources)，如下所示。

有关访问 MCP 功能的具体说明，请参阅您的 LLM 文档。

## 提示

提示是预配置的操作，允许您执行常见操作，例如创建新网格或迁移到更高版本。

AG MCP 目前提供两个提示：

- [`quick-start`](#create-a-new-grid) - 在任何框架中开始使用 AG Grid
- [`upgrade-grid`](#migrate-to-a-new-version) - 迁移到更新版本的 AG Grid

### 创建新网格

可以直接从您的 LLM 客户端触发 `quick-start` 提示。这会创建一个指令列表，供您的 LLM 在创建新的 AG Grid 项目或将 AG Grid 添加到现有项目时遵循。

您可以将额外的上下文、要求或指令作为参数传递给此提示，以微调您希望 LLM 创建的数据网格类型。

{% tabs %}
{% tabItem id="quick-start-cursor" label="Cursor" %}
要触发 `quick-start` 提示，在您的 LLM 提示中输入以下命令：

```bash
/ag-mcp:quick-start {additional-context}
```

{% /tabItem %}
{% tabItem id="quick-start-vs-code" label="VS Code (CoPilot)" %}
要触发 `quick-start` 提示，在您的 LLM 提示中输入以下命令：

```bash
mcp.ag-mcp.quick-start {additional-context}
```

{% /tabItem %}
{% tabItem id="quick-start-claude" label="Claude" %}
要触发 `quick-start` 提示，在您的 LLM 提示中输入以下命令：

```bash
/ag-mcp:quick-start {additional-context}
```

{% /tabItem %}
{% /tabs %}

### 迁移到新版本

`upgrade-grid` 提示创建一个分步计划，帮助从当前版本迁移到提供的版本。这会提供给 LLM 执行，根据需要回调 MCP 服务器。它采用逐版本的方法，确保每个版本正确后再继续。

要触发 `upgrade-grid` 提示，在您的 LLM 提示中输入以下命令：

{% tabs %}
{% tabItem id="migrate-cursor" label="Cursor" %}

```bash
/ag-mcp:upgrade-grid {additional-context}
```

{% /tabItem %}
{% tabItem id="migrate-vs-code" label="VS Code (CoPilot)" %}

```bash

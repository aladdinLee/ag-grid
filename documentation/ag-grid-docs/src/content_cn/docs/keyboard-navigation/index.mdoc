---
title: '键盘交互'
---

网格响应用户的键盘交互，并在网格单元格上发生按键时发出事件。下面显示了可以使用网格进行的所有键盘交互。

## 导航

使用箭头键（{% kbd "←" /%} {% kbd "↑" /%} {% kbd "→" /%} {% kbd "↓" /%}）向上、向下、向左和向右移动焦点。如果焦点单元格已经位于该位置的边界上（例如，如果在第一列并按下左键），则按键无效。使用 {% kbd "^ Ctrl" /%}+{% kbd "←" /%} 移动到行的开头，使用 {% kbd "^ Ctrl" /%}+{% kbd "→" /%} 移动到末尾。

如果焦点位于网格第一行的单元格上并按下 {% kbd "↑" /%}，焦点将移动到网格标题。标题导航焦点导航的工作方式与网格相同：箭头将向上/向下/向左/向右移动，{% kbd "⇥ Tab" /%} 将水平移动焦点，直到最后一个标题单元格，然后移动到下一行。

使用 {% kbd "Page Up" /%} 和 {% kbd "Page Down" /%} 向上和向下滚动一页。使用 {% kbd "Home" /%} 和 {% kbd "End" /%} 转到第一行和最后一行。

{% note %}
当标题单元格获得焦点时，{% kbd "Page Up" /%}、{% kbd "Page Down" /%}、{% kbd "Home" /%}、{% kbd "End" /%}、{% kbd "^ Ctrl" /%}+{% kbd "←" /%}/{% kbd "→" /%} 等命令将无法像网格单元格获得焦点时那样工作。
{% /note %}

## 分组

如果在组元素上，按 {% kbd "↵ Enter" /%} 键将展开或折叠组。

## 编辑

在单元格上按 {% kbd "F2" /%} 或 {% kbd "↵ Enter" /%} 键将使单元格进入编辑模式（如果允许编辑该单元格）。这适用于默认单元格编辑器。

在选择单元格范围并编辑其中一个单元格时按 {% kbd "^ Ctrl" /%}+{% kbd "↵ Enter" /%} 键，将编辑单元格的新值设置为所选范围内所有可编辑单元格

## 选择

在聚焦单元格时按 {% kbd "␣ Space" /%} 键将选择单元格的行，如果已选择则取消选择该行。如果启用了多选，此选择更改不会删除任何先前的选择。

## 禁止焦点

如果要关闭键盘导航，有两个属性需要关闭。

### 禁止单元格焦点

在 gridOptions 中设置 `suppressCellFocus=true`，将禁用网格单元格焦点。

### 禁止标题焦点

在 gridOptions 中设置 `suppressHeaderFocus=true`，将禁用网格标题焦点。

## 列标题导航

网格标题支持完整的键盘导航，但行为可能因当前聚焦的列标题类型而异。

列标题可以：

-   通过按 {% kbd "⇧ Shift" /%} + {% kbd "←" /%} / {% kbd "→" /%} 移动。
-   通过按 {% kbd "⌥ Alt" /%} + {% kbd "←" /%} / {% kbd "→" /%} 调整大小。

### 列组标题

在导航列组标题时，您可以执行以下操作：

-   如果当前列组可展开，按 {% kbd "↵ Enter" /%} 将切换组的展开状态。
-   当启用[列单元格选择](./cell-selection/#selecting-cells-via-column-headers)时：
    -   如果当前列组可展开，按 {% kbd "⌥ Alt" /%}+{% kbd "↵ Enter" /%} 将切换组的展开状态。
    -   按 {% kbd "↵ Enter" /%} 选择焦点列组的子列中的所有可见单元格。
    -   按 {% kbd "^ Ctrl" /%}+{% kbd "↵ Enter" /%} 选择焦点列组的子列中的所有可见单元格，而不清除现有单元格范围。
    -   按 {% kbd "⇧ Shift" /%}+{% kbd "↵ Enter" /%} 选择先前选择的列组和焦点列组之间可见子列中的所有单元格。
    -   按 {% kbd "^ Ctrl" /%}+{% kbd "⇧ Shift" /%}+{% kbd "↵ Enter" /%} 选择先前选择的列组和焦点列组之间可见子列中的所有单元格，而不清除现有单元格范围。

### 普通列标题

常规列标题可能有选择复选框、排序功能和菜单，因此要在聚焦列标题时访问所有这些功能，您可以执行以下操作：

-   按 {% kbd "␣ Space" /%} 切换列标题复选框选择。
-   按 {% kbd "↵ Enter" /%} 切换该列的排序状态。
-   按 {% kbd "⇧ Shift" /%}+{% kbd "↵ Enter" /%} 切换该列的多重排序。
-   按 {% kbd "⌥ Alt" /%}+{% kbd "↓" /%} 打开焦点列标题的菜单。
-   按 {% kbd "^ Ctrl" /%}+{% kbd "↵ Enter" /%} 打开焦点列标题的过滤器（如果 `columnMenu = 'new'` - 默认行为）或打开焦点列标题的菜单（如果 `columnMenu = 'legacy'`）。
-   当菜单打开时，只需按 {% kbd "⎋ Esc" /%} 关闭它，焦点将返回到列标题。
-   当启用[列单元格选择](./cell-selection/#selecting-cells-via-column-headers)时：
    -   按 {% kbd "↵ Enter" /%} 选择焦点列中的所有可见单元格。
    -   按 {% kbd "^ Ctrl" /%}+{% kbd "↵ Enter" /%} 选择焦点列中的所有可见单元格，而不清除现有单元格范围。
    -   按 {% kbd "⇧ Shift" /%}+{% kbd "↵ Enter" /%} 选择先前选择的列和焦点列之间可见列中的所有单元格。
    -   按 {% kbd "^ Ctrl" /%}+{% kbd "⇧ Shift" /%}+{% kbd "↵ Enter" /%} 选择先前选择的列和焦点列之间可见列中的所有单元格，而不清除现有单元格范围。
    -   按 {% kbd "⌥ Alt" /%}+{% kbd "↵ Enter" /%} 切换该列的排序状态。
    -   按 {% kbd "⌥ Alt" /%}+{% kbd "⇧ Shift" /%}+{% kbd "↵ Enter" /%} 切换该列的多重排序。

### 浮动过滤器标题

在使用键盘导航浮动过滤器列标题时，按 {% kbd "←" /%} {% kbd "→" /%} 将焦点从一个列标题移动到下一个。如果希望在浮动过滤器单元格内导航，请按 {% kbd "↵ Enter" /%} 聚焦当前浮动过滤器单元格内的第一个启用元素，然后按 {% kbd "⎋ Esc" /%} 将焦点返回到浮动过滤器列标题。

## 示例

下面的示例有分组标题、标题和浮动过滤器，以演示上述功能：

{% gridExampleRunner title="键盘导航" name="grid-keyboard-navigation" /%}

## 自定义导航

大多数人会对使用箭头键和 {% kbd "⇥ Tab" /%} 键时网格的默认导航感到满意。有些人会想要覆盖这一点（例如，您可能希望 {% kbd "⇥ Tab" /%} 键导航到下面的单元格，而不是右侧的单元格）。为此，网格提供了五个方法：`navigateToNextCell`、`tabToNextCell`、`navigateToNextHeader`、`tabToNextHeader` 和 `tabToNextGridContainer`。

{% apiDocumentation source="grid-options/properties.json" section="nav" names=["focusGridInnerElement", "navigateToNextCell", "tabToNextCell", "navigateToNextHeader", "tabToNextHeader", "tabToNextGridContainer"] /%}

{% note %}
`navigateToNextCell` 和 `tabToNextCell` 仅在跨网格单元格导航时调用，而 `navigateToNextHeader` 和 `tabToNextHeader` 仅在跨网格标题导航时调用。当 Tab 导航在核心网格容器之间移动时，会调用 `tabToNextGridContainer` 回调。如果需要从一个容器导航到另一个容器，请在 `CellPosition` 中传递 `rowIndex: -1` 或在 `HeaderPosition` 中传递 `headerRowIndex: -1`。
{% /note %}

## 示例自定义单元格导航

下面的示例展示了如何在实践中使用 `navigateToNextCell`、`tabToNextCell`、`navigateToNextHeader` 和 `tabToNextHeader`。

请注意以下几点：

-   `navigateToNextCell` 交换上下箭头键。
-   `tabToNextCell` 使用 Tab 键上下移动，而不是左右移动。
-   `navigateToNextHeader` 交换上下箭头键。
-   `tabToNextHeader` 使用 Tab 键上下移动，而不是左右移动。
-   当第一网格行中的单元格获得焦点时，按下箭头键将通过传递 `rowIndex: -1` 导航到标题。
-   当最后一个标题行中的标题单元格获得焦点时，按上箭头键将通过传递 `headerRowIndex: -1` 导航到第一网格行。
-   Tab 键/Shift Tab 键将移动焦点，直到第一个标题或最后一个网格行，但焦点不会离开网格。

{% gridExampleRunner title="自定义键盘导航" name="custom-keyboard-navigation" /%}

## 自定义主从导航

[主从网格](./master-detail/)可以包含具有自己渲染器的[自定义详细信息](./master-detail-custom-detail/)，因此需要实现自己的键盘导航。可以在[自定义详细信息键盘导航示例](./master-detail-custom-detail/#keyboard-navigation)中看到这方面的示例。

## Tab 键进入网格

在网格嵌入到较大页面中的应用程序中，默认情况下，当 Tab 键进入网格时，第一列标题将获得焦点。

您可以覆盖此行为以聚焦第一个网格单元格，如果这是首选方案，可以使用以下代码片段中显示的 DOM 事件监听器和 Grid API 调用的组合：

```{% frameworkTransform=true %}
// 获取输入元素的引用
const myInput = document.getElementById("my-input");

// 拦截输入元素内的按键
myInput.addEventListener("keydown", event => {
    // 忽略非 Tab 键按键
    if (event.key !== 'Tab') return;

    // 防止 Tab 键进入 url 部分
    event.preventDefault();

    // 滚动到第一行
    gridApi.ensureIndexVisible(0);

    // 滚动到第一列
    const firstCol = gridApi.getAllDisplayedColumns()[0];
    gridApi.ensureColumnVisible(firstCol);

    // 将焦点设置到第一个网格单元格
    gridApi.setFocusedCell(0, firstCol);

}, true);
```

### Tab 键进入网格

在以下示例中，提供了两个输入框来测试 Tab 键进入网格。请注意以下几点：

-   从网格上方的输入框 Tab 键移出将聚焦第一个网格标题。
-   当第一个单元格由于向下滚动（行）或横向滚动（列）而不可见时，网格将向左滚动以显示第一列。
-   按下 Shift 键从网格下方的输入框 Tab 键移出将聚焦网格的最后一个单元格。
-   当最后一列由于水平滚动而不可见时，Shift Tab 键进入网格将导致网格滚动以聚焦最后一个单元格。

{% gridExampleRunner title="Tab 键进入网格" name="tabbing-into-grid" /%}

### 在网格容器之间自定义 Tab 键

使用 `tabToNextGridContainer` 覆盖在网格容器（如网格主体、分页工具栏和外部元素）之间 Tab 键移动时的焦点行为。

在以下示例中：

-   从最后一个网格单元格 Tab 键移出通过返回 `'pagination'` 重定向到分页工具栏。
-   从分页工具栏 Shift Tab 键返回网格通过返回 `CellPosition` 恢复最后聚焦的单元格。
-   从分页向前 Tab 键返回 `false`，允许浏览器默认行为将焦点移出网格。
-   所有其他转换返回 `undefined`，保持网格的默认行为。

{% gridExampleRunner title="自定义 Tab 键到下一个网格容器" name="tab-to-next-grid-container" /%}

### 自定义 Tab 键进入网格

`focusGridInnerElement` 回调可用于更改网格从外部接收焦点时聚焦的元素。请注意以下几点：

-   从网格上方的输入框 Tab 键移出将聚焦最后聚焦的单元格（如果网格之前已聚焦且元素仍在 DOM 中），否则聚焦标题。
-   从网格下方的输入框 Shift Tab 键移出将聚焦最后聚焦的单元格（如果网格之前已聚焦且元素仍在 DOM 中），否则聚焦底行的最后一个单元格。

{% gridExampleRunner title="自定义 Tab 键进入网格" name="custom-tabbing-into-grid" /%}

## 键盘事件

可以使用网格事件 `cellKeyDown`（当单元格上触发 DOM `keyDown` 事件时调用）为您想要的任何键事件添加自定义行为。

{% note %}
这些键盘事件由网格面板监视，因此当 `keydown` 发生在弹出编辑器内时不会触发，因为弹出元素在不同的 DOM 树中渲染。
{% /note %}

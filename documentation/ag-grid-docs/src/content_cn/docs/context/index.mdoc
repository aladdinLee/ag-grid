---
title: "上下文"
---

本节介绍如何在网格中传递共享的上下文信息。

## 概述

上下文对象被传递给网格中使用的所有回调和事件。上下文对象的目的是允许客户端应用程序将详细信息传递给自定义回调，例如[单元格渲染器](./component-cell-renderer/)和[单元格编辑器](./cell-editing/)。

{% apiDocumentation source="grid-options/properties.json" section="miscellaneous" names=["context"] /%}

要更新上下文，请使用新的上下文调用 `api.setGridOption`。或者，如果您维护对上下文对象的引用，可以直接修改其值。

## 上下文对象示例

下面的示例演示了如何使用上下文对象。请注意以下内容：

-   从下拉列表中选择报告货币会将其放入上下文对象中。

-   当报告货币更改时，单元格渲染器使用上下文对象中提供的货币来计算值，使用：`params.context.reportingCurrency`。

-   价格列表头使用表头值获取器更新以显示选定的货币，使用 `ctx.reportingCurrency`。

{% gridExampleRunner title="上下文对象" name="context" /%}

## 上下文和表达式示例

下面显示了一个复杂的示例，使用了值获取器（使用表达式）和类规则（同样使用表达式）。网格显示按城市和国家/地区划分的小部件销售的"实际与预算数据和年度总计"。

-   **Location** 列显示聚合组，按城市和国家/地区分组。
-   **Monthly Data** 列受上下文影响。根据选定的期间，显示的数据是该月的实际（`x_act`）或预算（`x_bud`）数据（例如，当 Jan 为绿色时为 `jan_act`，当 Jan 为红色时为 `jan_bud`）。同样，背景颜色也使用依赖于选定期间的类规则进行更改。
-   **sum(YTD)** 是"实际"数字的总和，即所有绿色数字的总和。这也会随着期间的更改而更改。

请注意，该示例（包括动态计算表达式，网格只计算需要显示的内容）运行非常快（一旦加载数据），尽管有超过 6,000 行。

最好在新标签页中打开此示例进行查看。

{% gridExampleRunner title="月度销售" name="monthly-sales" /%}

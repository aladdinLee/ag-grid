---
title: "DOM 虚拟化"
---

网格使用 DOM 虚拟化来大幅提高渲染性能。

如果您在不使用数据网格的情况下将 1,000 条记录和 20 列加载到浏览器中（例如使用 `<table>`、`<tr>` 和 `<td>` 标签），则页面最终会有大量渲染的 DOM 元素。这将大大减慢网页速度。这会导致非常糟糕的用户体验，或者简单地使浏览器崩溃，因为浏览器内存不足。

为了解决这个问题，网格只渲染您在屏幕上看到的内容。例如，如果您将 1,000 条记录和 20 列加载到网格中，但用户只能看到 50 条记录和 10 列（因为其余记录未滚动到视图中），则网格只渲染用户实际可以看到的 50 行和 10 列。

当用户水平或垂直滚动时，网格会动态更新 DOM 并渲染所需的额外单元格，同时删除不再可见的单元格。

这种只将可见滚动视口中的内容渲染到 DOM 中的技术称为行和列虚拟化。

## 检查 DOM

要观察行和列虚拟化，您可以使用浏览器的开发者工具检查网格的 DOM，并注意行和列 DOM 元素（即 'div' 元素）随着网格滚动而插入和删除。

## 行缓冲区

默认情况下，网格将在第一个可见行之前渲染 10 行，在最后一个可见行之后渲染 10 行，因此会渲染 20 个额外的行。这是作为缓冲区，因为在某些较慢的机器和浏览器上，用户滚动时可以看到空白空间。

要更改行缓冲区，请将网格属性 `rowBuffer` 设置为您希望在可见行之外渲染的行数。设置 `rowBuffer=0` 以关闭行缓冲。

行缓冲区值用于根据行缓冲区值乘以默认行高创建像素范围。例如，如果您设置 `rowBuffer=10` 且默认行高为 42px（对于 Quartz 主题），网格将在两个方向上将垂直像素范围扩展 420px，然后将行绘制到其中。

当使用动态和自动行高时，此细节很重要，因为根据实际行高，不同数量的行可以适应像素范围，因为其高度基于默认行高。

例如，如上所述，如果 `rowBuffer=10`，网格将在两个方向上使用 420px 的垂直像素范围。但是，如果您设置自定义行高，或者自动高度设置行高为 100px（而不是默认的 42px），则网格上方和下方的这两个 420px 像素范围中的每一个只能容纳 5 个这些 100px 行，而不是 10 个默认高度为 42px 的行。

## 最大渲染行数

作为安全措施，网格最多渲染 500 行。这是为了防止应用程序
在错误设置网格大小时崩溃。例如，如果网格高度设置不正确，
并且网格尝试渲染 10,000 行，这可能会使浏览器崩溃。要删除
此限制，请设置属性 `suppressMaxRenderedRowRestriction=true`。

## 列虚拟化

列虚拟化是随着网格水平滚动而插入和删除列。

没有列缓冲区 - 除了可见集合之外，不渲染额外的列。这是因为水平滚动不像垂直滚动那样占用 CPU，因此缓冲区对于良好的 UI 体验不需要。

## 禁止虚拟化

要关闭列虚拟化，请设置网格属性 `suppressColumnVirtualisation=true`。要关闭行虚拟化，请设置网格属性 `suppressRowVirtualisation=true`。

请注意，如果 `suppressRowVirtualisation=true`，则没有行缓冲区，并且 `rowBuffer` 属性将被忽略。

下面的示例演示了这两个属性。

-   请注意，每个单元格都使用值格式化器，该格式化器在使用时打印到控制台，这相当于每次创建单元格时。

-   打开控制台，请注意，当网格渲染时，值格式化器被调用 900 次，因为有 900 个数据单元格（9 列 x 100 行）。请注意，滚动网格不再调用值格式化器。

-   如果您要编辑此示例，删除 `suppressColumnVirtualisation` 和 `suppressRowVirtualisation`，然后重新运行它，您会看到行为的变化。打开控制台，注意单元格只在网格滚动时创建，值格式化器被调用。

{% gridExampleRunner title="禁止虚拟化" name="suppress-virtualisation" /%}

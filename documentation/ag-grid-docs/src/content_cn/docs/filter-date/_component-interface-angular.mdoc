{% if isFramework("angular") %}
自定义日期组件的接口如下：
{% /if %}

{% if isFramework("angular") %}
{% interfaceDocumentation interfaceName="IDateAngularComp" config={"asCode":true } /%}
{% /if %}

{% if isFramework("angular") %}
`agInit(params)` 方法接受一个包含以下项目的参数对象：
{% /if %}

{% if isFramework("javascript") %}
自定义日期组件的接口如下：
{% /if %}

{% if isFramework("javascript") %}
{% interfaceDocumentation interfaceName="IDateComp" config={"asCode":true } /%}
{% /if %}

{% if isFramework("javascript") %}
`init(params)` 方法接受一个包含以下项目的参数对象：
{% /if %}

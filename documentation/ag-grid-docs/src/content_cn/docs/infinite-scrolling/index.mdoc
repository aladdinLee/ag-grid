---
title: "无限行模型"
---

{% note %}
如果您是企业用户，您应该考虑使用[服务端行模型](./server-side-model/)而不是无限行模型。
它提供相同的功能以及更多功能。
行模型之间的差异可以在我们的[行模型摘要页面](./row-models/)中找到。
{% /note %}

无限滚动允许网格根据网格的滚动位置从服务器延迟加载行。在其最简单的形式中，用户向下滚动越多，加载的行就越多。

网格将有一个"自动扩展"的垂直滚动。这意味着当滚动到达底部位置时，网格将扩展高度以允许进一步向下滚动，几乎使用户无法到达底部。一旦网格扩展滚动到达表中的最后一条记录，这就会停止。

## 工作原理

网格将通过数据源向您的应用程序请求块中的行。每个块包含整个数据集的行子集。下图是高级概述。

{% imageCaption imagePath="resources/high-level.png" alt="high-level" centered=true constrained=true enableDarkModeFilter=true /%}

当网格滚动到没有加载相应行块的位置时，模型使用提供的数据源获取请求块的行。在图中，数据源从远程服务器的数据库中获取行。

## 启用无限滚动

要启用无限滚动，您必须 a) 将网格属性 `rowModelType` 设置为 `'infinite'` 并且 b) 提供数据源。

```js
// 网格初始化之前
gridOptions.rowModelType = 'infinite';
gridOptions.datasource = myDataSource;

// 网格初始化后，您可以设置或更改数据源
api.setGridOption('datasource', myDataSource);
```

## 数据源

必须提供数据源才能进行无限滚动。您可以将数据源指定为网格属性或使用网格 API。

```js
// 设置为属性
gridOptions.datasource = myDatasource;

// 或在网格初始化后使用 api
api.setGridOption('datasource', myDatasource);
```

### 更改数据源

在网格初始化后更改数据源将重置网格中的无限滚动。
如果数据的上下文发生变化，这很有用，即如果您想查看不同的数据集。

{% note %}
如果您调用 `setGridOption('datasource', datasource)`，网格将假设它是新数据源，重置块缓存。但是您可以传入相同的数据源实例。
因此，例如，您的应用程序可能有一个数据源实例，该实例知道某些外部上下文（例如，为报告选择的业务日期，或您连接到的"银行 ATM 实例"数据），当上下文更改时，您想重置，但仍保持相同的数据源实例。在这种情况下，只需调用 `setGridOption('datasource', datasource)` 并再次传入相同的数据源。
{% /note %}

### 数据源接口

简而言之，每次网格想要更多行时，它都会在数据源上调用 `getRows()`。数据源用请求的行响应。您的无限滚动数据源应该实现 `IDatasource` 接口：

{% interfaceDocumentation interfaceName="IDatasource" config={"overrideBottomMargin":"1rem"} /%}

`getRows()` 方法接受 `IGetRowsParams` 参数：

{% interfaceDocumentation interfaceName="IGetRowsParams" /%}

### getRows()

`getRows()` 函数由网格调用，用于将行块加载到浏览器的块缓存中。
它接受以下参数：

-   `startRow` 和 `endRow` 定义调用期望的范围。例如，如果使用 `startRow: 0` 和 `endRow: 100` 调用 `getRows` 函数，则网格将期望结果有 100 行（第 0 到 99 行）。

-   `successCallback(rowsThisBlock, lastRow)` 应在您成功从服务器接收数据时调用。回调具有以下参数：

    -   `rowsThisBlock` 应该是您为当前块接收的行。
    -   `lastRow` 应该是最后一行的索引（如果已知）。

-   如果加载失败，应该调用 `failCallback()`。应该恰好调用 `successCallback()` 或 `failCallback()` 之一一次。

-   传递 `filterModel()` 和 `sortModel()` 用于进行服务端排序和过滤。

-   [上下文](./context/)只是按原样传递，与无限滚动无关。如果您需要将应用程序状态提供给数据源，它就在那里。

### 设置最后一行索引

成功回调参数 `lastRow` 用于将网格移出无限滚动。如果最后一行已知，则这应该是最后一行的索引。如果最后一行未知，则留空（`undefined`、`null` 或 `-1`）。此属性仅在无限滚动时使用。一旦总记录数已知，`lastRow` 参数将被忽略。

在正常操作下，每次调用 `getRows()` 时，您都会为 `lastRow` 返回 `null` 或 `undefined`，除了到达最后一个块时。例如，如果块大小为 100 且您有 250 行，当第三次调用 `getRows()` 时，您将在结果中返回 50 行并将 `rowCount` 设置为 250。这将使网格设置滚动条以精确适应 250 行，并且不会请求更多块。

## 块缓存

网格将块保留在缓存中。您可以选择永不过期块，或设置保留块数量的限制。如果您设置限制，那么当您向下滚动时，以前的块将被丢弃，如果用户向上滚动，将再次加载。缓存中保留的最大块数使用 `maxBlocksInCache` 属性设置。

### 块大小

块大小使用网格属性 `cacheBlockSize` 设置。这是缓存中每个块应包含的行数。对数据源的每次调用都将是一个块。

### 防抖块加载

也可以防抖加载以防止在滚动停止之前加载块。可以使用 `blockLoadDebounceMillis` 进行配置。

### 聚合和分组

无限滚动中不提供聚合和分组。这是因为这样做需要网格知道整个数据集，这在使用无限行模型时是不可能的。如果您需要大数据集的聚合和/或分组，请查看[服务端行模型](./server-side-model/)以在服务端进行聚合。

### 排序和过滤

网格无法为您进行排序或过滤，因为它没有所有数据。排序或过滤必须在服务端完成。因此，如果排序或过滤器更改，网格将使用数据源再次获取数据并向您提供排序和过滤状态。

### 简单示例：无排序或过滤

下面的示例使用无限滚动和缓存。请注意，当您将滚动条一直带到底部时，网格将加载更多数据。

{% gridExampleRunner title="简单示例" name="simple" /%}

### 选择

选择通过使用行节点的[行 ID](./row-ids/) 在无限滚动中的行上工作。如果您不为行节点提供键，则将使用行节点的索引。当（服务端）过滤或排序时，使用行的索引会出问题，因为这些会更改行的索引。因此，如果您不提供自己的[行 ID](./row-ids/)，那么如果排序或过滤器更改，选择将被清除。

要提供自己的[行 ID](./row-ids/)，请实现方法 `getRowId(params)`，该方法应返回数据的键。

```js
gridOptions.getRowId: function(params) {
    // ID 可以是任何字符串，只要它在您的数据集中是唯一的
    return params.data.id.toString();
}
```

一旦实现了 `getRowId()`，选择将在排序和过滤器之间持久存在。

{% note %}
无限行模型没有内置的[全选](./row-selection-multi-row/#selecting-all-rows)功能支持。单击标题复选框或按 {% kbd "^ Ctrl" /%}+A 都不会有任何效果。
{% /note %}

### 示例：排序、过滤和选择

以下示例通过添加服务端排序、过滤和持久选择扩展了上面的示例。

任何列都可以通过单击标题进行排序。发生这种情况时，将使用新的排序选项再次调用数据源。

列 `Age`、`Country` 和 `Year` 可以过滤。发生这种情况时，将使用新的过滤选项再次调用数据源。

当选择一行时，选择将保留在网格内，即使网格被排序或过滤。请注意，当网格加载选定的行时（例如，选择第一行，向下滚动以便第一个块从缓存中删除，然后再次向上滚动），该行在从服务器加载行之前不会突出显示。这是因为网格正在等待查看要加载的行的 ID。

{% note %}
下面的示例使用 AG Grid Enterprise，以演示具有服务端过滤的设置过滤器。无限滚动不需要 AG Grid Enterprise。
{% /note %}

{% gridExampleRunner title="服务端排序和过滤" name="server-side" /%}

{% note %}
使用 shift-click 执行多行选择时，如果 `maxBlocksInCache` 的配置值未覆盖该范围，则可能并非所有行都在内存中可用。在这种情况下，将不允许进行多项选择。
{% /note %}

## 指定可选择的行

也可以通过 `rowSelection.isRowSelectable` 回调函数指定哪些行可以被选择。

{% interfaceDocumentation interfaceName="MultiRowSelectionOptions" names=["isRowSelectable"] config={"description":""} /%}

例如，如果我们只想允许 `data.country` 属性为 'United States' 的行，我们可以实现以下内容：

```{% frameworkTransform=true %}
const gridOptions = {
    rowSelection: {
        mode: 'multiRow',
        isRowSelectable: function(data) {
            return data.country === 'United States';
        }
    }
}
```

{% gridExampleRunner title="指定可选择的行" name="specify-selectable-rows" /%}

请注意，在上面的示例中，我们隐藏了禁用的复选框以帮助突出显示哪些行是可选择的。

### 稍有不同的配置

上面的示例使用旧式 JavaScript 对象作为数据源。这个示例稍微改变了情况并创建了一个数据源类。该示例也只是即时生成数据。

{% gridExampleRunner title="模拟数据" name="made-up-data" /%}

### 加载微调器

此页面上的示例使用加载微调器显示行是否正在等待其数据加载。网格不提供此功能，而是示例中使用的简单渲染技术。如果数据正在加载，则 `rowNode` 将没有 ID，因此如果您使用 ID 作为值，则在设置 ID 时单元格将刷新。

```js
loadingSpinnerColumn = {
    // 使用值获取器将节点 ID 作为此列的值
    valueGetter: 'node.id',

    // 然后是自定义 cellRenderer
    cellRenderer: function(params) {
        if (params.value === undefined) {
            // 当没有节点 id 时，显示微调器图像
            return '<img src="loading.gif" />';
        } else {
            // 否则只显示节点 ID（或您希望为此列显示的任何内容）
            return params.value;
        }
    }
}
```

有关如何构建单元格渲染器，请参阅[单元格渲染](./component-cell-renderer/)部分。

### 通过属性和 API 进行更多控制

无限滚动在幕后有一个缓存工作。提供以下属性和 API 让您控制缓存。

### 属性

{% apiDocumentation source="grid-options/properties.json" section="infiniteRowModel" names=["cacheOverflowSize", "maxConcurrentDatasourceRequests", "maxBlocksInCache", "infiniteInitialRowCount", "cacheBlockSize"]  config={"restrictModule":"InfiniteRowModelModule"} /%}

### API

{% apiDocumentation source="grid-api/api.json" section="infiniteScrolling" config={"restrictModule":"InfiniteRowModelModule"} /%}

{% note %}
不建议在无限滚动的网格中直接添加/删除行，因为这会使您的应用程序复杂化。如果您更新服务器上的数据并刷新块缓存，这会让您的生活更轻松。
{% /note %}

### 示例：使用缓存 API 方法

下面通过按钮演示不同的 API 方法。该示例向控制台输出许多调试项，因为设置了网格属性 `debug=true`。按钮如下：

-   **Insert Rows**：从服务器的行索引 2 处插入 5 行，然后刷新网格。
-   **Delete Rows**：从服务器的行索引 3 处删除 10 行，然后刷新网格。
-   **Set Row Count**：将行数设置为 200。这会调整垂直滚动以显示 200 行。如果滚动条位于末尾，这将导致网格自动重新调整，因为它会寻找下一个数据块。
-   **Print Info**：将 `rowCount` 和 `maxFound` 打印到控制台。
-   **Jump to 500**：定位网格以显示第 500 行。
-   **Print Cache State**：调试方法，用于查看缓存的状态。
-   **Set Prices High & Set Prices Low**：将服务器端的价格设置为高或低价格。在加载块缓存之前，这不会影响网格。使用这些按钮然后进一步测试刷新和清除方法。
-   **Refresh Cache**：调用刷新缓存。
-   **Purge Cache**：调用清除缓存。

该示例还使每个 Honda 行加粗 — 演示回调 `getRowStyle` 和 `getRowClass` 在设置数据后以及创建行时（此时数据可能尚不可用）都会被调用。

{% gridExampleRunner title="插入和删除示例" name="insert-remove" /%}

## 更改列

使用无限滚动可以[更改列](./column-updating-definitions/)，它不需要再次从服务器获取数据。如果列的更改影响排序或过滤器（即，删除了应用了排序或过滤器的列），网格将再次获取数据，类似于用户显式更改排序或过滤器后获取数据的方式。

下面的示例演示在无限行模型上更改列。可以注意到以下几点：

-   点击按钮 'Show Year' 和 'Hide Year'。请注意，数据没有重新获取。
-   向 Age 列添加排序或过滤器。应用排序或过滤器时，数据会重新获取。但是，一旦获取，您可以添加和删除 Year 列而无需重新获取数据。
-   向 Year 列添加排序或过滤器。应用排序或过滤器时，数据会重新获取。现在删除 Year 列，数据会再次重新获取，因为排序或过滤器已更改。

{% gridExampleRunner title="更改列" name="changing-columns" /%}

## 分页

与所有行模型一样，可以启用无限滚动的分页。使用无限滚动，可以混合和匹配配置以实现不同的效果。呈现以下示例：

| 示例   | 页面大小 | 块大小 | 注释     |
| ------ | -------- | ------ | -------- |
| 示例 1 | 自动     | 大     | 最推荐   |
| 示例 2 | 相等     | 相等   | 有时推荐 |

{% note %}
**无限块大小小于分页页面大小不受支持**

您必须使无限块大小大于或等于分页页面大小。如果块大小较小，网格将无法获取足够的行来显示一页。这会破坏无限滚动的工作方式，不受支持。
{% /note %}

### 示例 1：自动分页页面大小，大无限块大小

此示例是推荐的方法。无限块大小大于页面大小，因此网格加载几页的数据，允许用户在需要服务端调用之前点击"下一页"几次。

{% gridExampleRunner title="块大于页面" name="block-larger-page"  exampleHeight=615 /%}

### 示例 2：相等的分页页面大小和大无限块大小

此示例演示页面和块大小相等。在这里，每次导航到新页面时都会访问服务器。

{% gridExampleRunner title="块等于页面" name="block-equal-page"  exampleHeight=615 /%}

## 覆盖层

无限行模型不会自动显示 `loading` 覆盖层，因为行是在用户与网格交互时以块的形式加载的。如果数据源返回空数组，网格将显示 `no rows` 覆盖层，如果在设置过滤器时返回空数组，则显示 `no matching rows` 覆盖层。

有关配置详细信息，包括抑制内置覆盖层，请参阅[覆盖层](./overlays-overview/)。

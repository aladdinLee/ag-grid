---
title: "AG Grid 模块"
description: "通过精挑细选表格功能来减少应用程序包大小。使用模块选择器选择您想要使用的功能并生成代码。"
---

使用 AG Grid 模块通过精挑细选表格功能来减少应用程序包大小，使用模块选择器生成注册代码，并使用验证模块验证您的设置。

{% if isFramework("javascript") %}
{% note %}
如果您使用 UMD 包，则不需要注册模块，因为这已经自动完成。
{% /note %}
{% /if %}

## 概述

AG Grid 模块允许您通过只包含想要使用的模块来减少应用程序的包大小。使用下面的[模块选择器](#selecting-modules)工具选择您想要使用的模块并生成正确的模块注册代码。

或者，如果您不关心包大小，并且想要访问社区/企业版中的每个功能，请使用[包](#bundles)来包含所有社区/企业模块。

## 包

表格提供两个模块包以便使用：

-   `AllCommunityModule` 包包含 AG Grid 社区版中可用的所有模块。
-   `AllEnterpriseModule` 包包含 `AllCommunityModule` 和 AG Grid 企业版中可用的所有模块。

注册其中一个包可以复制 33 版本之前 AG Grid 包版本的行为：

{% if isFramework("react") %}

```jsx
import { AllCommunityModule } from 'ag-grid-community';
// import { AllEnterpriseModule } from 'ag-grid-enterprise';
import { AgGridProvider, AgGridReact } from 'ag-grid-react';

const modules = [AllCommunityModule]; // 或 [AllEnterpriseModule]

// 使用 AgGridProvider 包装您的应用程序（或使用 AG Grid 的部分）
function App() {
    return (
        <AgGridProvider modules={modules}>
            <AgGridReact /* ... props */ />
        </AgGridProvider>
    );
}
```

{% /if %}

{% if isFramework("javascript", "angular", "vue") %}

```js
import {
    ModuleRegistry,
    AllCommunityModule
} from 'ag-grid-community';
// import { AllEnterpriseModule } from 'ag-grid-enterprise';

// 注册模块
ModuleRegistry.registerModules([
    AllCommunityModule, // 或 AllEnterpriseModule
]);
```

{% /if %}

如果您使用[集成图表](./integrated-charts/)或[迷你图](./sparklines-overview/)，则需要从 AG Charts 向 `AllEnterpriseModule` 提供相关模块，例如：

{% if isFramework("react") %}

```jsx
import { AgChartsEnterpriseModule } from 'ag-charts-enterprise';
import { AllEnterpriseModule } from 'ag-grid-enterprise';
import { AgGridProvider, AgGridReact } from 'ag-grid-react';

// 所有企业功能，包括集成图表和迷你图
const modules = [AllEnterpriseModule.with(AgChartsEnterpriseModule)];

function App() {
    return (
        <AgGridProvider modules={modules}>
            <AgGridReact /* ... props */ />
        </AgGridProvider>
    );
}
```

{% /if %}

{% if isFramework("javascript", "angular", "vue") %}

```js
import { AgChartsEnterpriseModule } from 'ag-charts-enterprise';
import { ModuleRegistry } from 'ag-grid-community';
import { AllEnterpriseModule } from 'ag-grid-enterprise';

// 所有企业功能，包括集成图表和迷你图
ModuleRegistry.registerModules([
    AllEnterpriseModule.with(AgChartsEnterpriseModule),
]);
```

{% /if %}
